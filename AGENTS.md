# 拓浙 AI 生态数字平台项目规则

> 历史工程名为“拓途浙享”，代码、域名和兼容接口中的 `tuotuzju` 可以继续保留。
>
> 本文件继承通用 `AGENTS.md`，只补充当前项目特有的认知、执行规则和验收案例。

## 0、整体架构

```text
项目 AGENTS.md
├── 一、项目认知：平台是什么，哪些边界不能破坏
├── 二、执行规则：本项目如何开发、回写和交付
├── 三、验收规则：本项目已经验证有效的验收案例
└── 四、事实与规则维护：把项目经验回写到正确位置
```

项目经验不单独建立章节：产品和技术经验进入项目认知，开发经验进入执行规则，验收教训进入验收规则。

## 一、项目认知

### 1.1 产品定位

拓浙 AI 生态数字平台从浙江大学及相关创新网络出发，连接学生、组织、学院、企业、真实 AI 需求与产业机会。它是拓浙 AI 生态的数字底座和公共入口，不是活动聚合站、新闻站或数字艺术展示站。

```text
机会发现
→ 人与组织建立可信连接
→ 社区学习、项目实践与浙客松验证
→ 身份、过程、作品、成果和关系沉淀
→ 人才识别、组织协作与下一轮机会
```

公开品牌优先使用“拓浙 AI 生态”。只有需要区分数字平台、线下生态、商业交付或学校职责时，才使用“拓途浙享”说明数字载体。

### 1.2 项目真源

| 需要了解的内容                   | 当前文档                                    |
| -------------------------------- | ------------------------------------------- |
| 项目上手                         | `README.md`                                 |
| 产品定义、闭环、模块和边界       | `docs/产品蓝图.md`                          |
| 当前能力、主要缺口和下一步       | `docs/产品状态.md`                          |
| 代码、数据、运行链和部署         | `docs/技术架构.md`                          |
| 当前详细工程护栏                 | `docs/开发禁区.md`                          |
| 发布、采集、打包和运维方法       | `docs/指南/`                                |
| 高影响任务的方案、任务和验收合同 | `openspec/changes/<change>/`                |
| 稳定功能合同                     | `openspec/specs/` 与正式 spec               |
| 已退役的方案和决策               | `docs/归档/` 与 `openspec/changes/archive/` |

### 1.3 稳定边界

| 领域       | 必须保持的边界                                                                                                                              |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| 产品       | 活动、AI 社区、项目广场、浙客松、profile、搜索推荐和后台属于同一个生态转化系统                                                              |
| 内容       | AI 社区的网页、文档、CLI 和微信采集进入共享审核与归属链，不建第二套内容真源                                                                 |
| 项目与成果 | 项目是一等业务对象；帖子、收藏、海报和媒体文件不能代替项目、赛事、主体和成果关系                                                            |
| 身份与权限 | `users` 是登录账号，`profiles + profile_members` 是个人和组织主体；`account_type`、`review_permission`、`role/admin_scope` 分别表达不同权限 |
| 合作关系   | 组织 profile、活动提供方和生态合作伙伴分别判断；对外背书只消费明确的 partner scope                                                          |
| 搜索与推荐 | 全站搜索负责跨模块查询，活动推荐负责画像驱动的机会判断，两者不合并                                                                          |
| AI 与索引  | `resource_search_index.vector_json` 是轻量词元表示，不宣称为 embedding 或 RAG；AI 只产生建议、条件或候选，不直接写正式业务事实              |
| 数据       | Schema 由核心建表与正式迁移决定；不在 controller 或热请求路径临时改表                                                                       |
| 多端       | Web/PWA 是产品和代码主真源；微信、Android 和 iOS 复用同一能力与用户入口                                                                     |
| 部署       | `master` 推送触发 GitHub Actions，生产入口是 Caddy；不恢复已退役的 Nginx 步骤                                                               |

## 二、执行规则

### 2.1 执行方式

| 方式          | 本项目中的适用情况                                                                               |
| ------------- | ------------------------------------------------------------------------------------------------ |
| 快速执行      | 文案、文档、局部样式、单点错误和不改变正式合同的小范围修改                                       |
| OpenSpec 执行 | 核心业务链、跨模块改造、数据库迁移、身份权限、审核归属、API 或状态合同、多端交付和其他高影响任务 |

已有 OpenSpec 覆盖任务时，继续当前 change，不重建平行方案。历史 OpenSpec 只用于追溯，不覆盖当前代码和正式 spec。

### 2.2 开发规则

1. 新能力先确定业务实体、正式真源、状态所有者、权限、审核、退出和恢复，再设计页面、组件和 API。
2. 新页面和入口复用已有主体、内容、项目、审核和权限链，不复制业务事实。
3. 数据库变化进入核心 schema 或正式 migration，并考虑幂等、备份、回退和失败可见性。
4. 上传、报名、收藏、审核和迁移不默认重试；只有领域幂等与重复回执明确时才能重试写操作。
5. 用户可见文案通过现有 i18n 同时维护中英文。
6. 多端保持一个用户入口，再根据 Web、PWA、微信、Android 或 iOS 运行态走对应桥接和降级链。
7. 数据库、备份、上传目录、密钥、签名、用户资料、商业文书和本地生成物不得进入公开 Git。

### 2.3 结果回写

| 稳定改变                               | 回写位置                  |
| -------------------------------------- | ------------------------- |
| 产品定位、闭环、长期模块和产品边界     | `docs/产品蓝图.md`        |
| 能力成熟度、主要缺口和当前主攻         | `docs/产品状态.md`        |
| 代码、数据、运行链、部署和稳定技术约束 | `docs/技术架构.md`        |
| 高影响变更的设计、任务和验收条件       | 对应 OpenSpec             |
| 发布、采集、打包或运维方法             | `docs/指南/` 中的对应手册 |
| 尚未形成稳定规则的真实结果             | 本地 `docs/事实.md`       |

### 2.4 Git 与交付

- 只暂存本任务文件，不使用 `git add -A`，不强推。
- 提交前检查 `git diff --cached --check` 和暂存范围。
- 用户未要求只保留本地时，任务闭环后安全对齐并推送 `origin/master`；不覆盖远端新提交。
- 推送 `master` 会触发生产部署。只有已授权发布并通过必要验收时，才把结果说成已交付。

## 三、验收规则

继承通用文件的“最小充分验收”。下表只是项目专项案例，每次只执行与改动直接相关的行。

| 改动场景                    | 默认验收                                                                                       |
| --------------------------- | ---------------------------------------------------------------------------------------------- |
| `AGENTS.md` 或普通 Markdown | 检查目标 diff、Markdown 结构和 `git diff --check`；不启动站点、浏览器或截图                    |
| 项目认知文档                | 检查文档职责、链接和当前代码或状态证据；不跑无关的工程测试                                     |
| locale 或用户文案           | 检查中英文对应项、JSON 解析和异常编码；只在显示逻辑受影响时打开页面                            |
| 局部前端逻辑                | 运行受影响文件的 ESLint 或相关测试；模块、路由或打包边界改变时再补 `npm run build`             |
| UI 外观或布局               | 打开真实受影响页面，只检查相关断点和主题，只对受影响的页面和设备截图                           |
| 路由、动画或深链            | 验证 URL、新页面可见、返回链与 query/hash；页面卡住时优先检查路由级 `AnimatePresence`          |
| PWA 缓存或同域子应用        | 验证直接请求和被旧 Service Worker 控制的标签页导航，检查子应用 denylist                        |
| 后端 API、权限或 schema     | 运行相关领域测试，核对后端权限、迁移与失败合同                                                 |
| AI 能力                     | 根据改动选择结构检查、golden、live、压力或恢复测试；Mock 和 golden 不代表真实 provider 通过    |
| 微信采集                    | 分别报告登录、抓取、解析、审核和发布状态，不用登录健康代替采集成功                             |
| 微信、Android 或 iOS        | 只验证受影响平台；发布级结论必须有对应工具、模拟器或真机证据                                   |
| OpenSpec 变更               | 验证对应 change 和相关测试；只在正式 spec 或多个 change 受影响时跑 `npm run openspec:validate` |
| 生产发布                    | 确认 GitHub Actions、Caddy 和服务状态，再做受影响的 HTTPS 或业务 smoke                         |

汇报时分清已通过、未运行、原本存在的失败、外部系统证据和仍需用户或真实环境确认的部分。

## 四、事实与规则维护

1. 本地 `docs/事实.md` 只记录值得保留的真实任务、动作、用户反馈、结果和证据，不直接写长期规则。
2. 经过相似场景验证的项目经验，合并到本文件最相关的认知、执行或验收位置，不建立 `docs/经验.md`。
3. 能被测试、lint、Hook、CI 或领域合同完整保证的约束，优先交给机器执行，不继续加重本文件。
4. `docs/开发禁区.md` 的现有详细护栏仍按需读取；当已被本文件、正式合同或自动化完整覆盖时，合并或删除重复内容，不长期双向维护。
5. 可迁移到多个项目的经验提议升级到通用 `AGENTS.md`；本文件不复制通用规则。
6. 新规则优先替换、合并或删除旧规则。当前进度和易变事实留在 `docs/产品状态.md`，不复制进本文件。
