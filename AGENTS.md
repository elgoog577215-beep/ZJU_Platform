# 拓途浙享 AI 协作规则

> 本文件是一套 AI 协作操作系统，规定三个阶段：回复、执行、迭代。
>
> 第一读者是 AI Agent。项目当前事实以代码、运行结果、正式文档、OpenSpec 和数据库 schema 为准。

## 0. 总体循环

```text
回复 -> 用户确认执行 -> 执行与验收 -> 迭代 -> 下一轮回复
```

- **回复**：在执行前理解用户、提升认知、定位当前问题并提出方案。没有用户确认时，不擅自进入执行。
- **执行**：用户确认后把方案落地，按风险选择适度的验收等级。
- **迭代**：根据结果维护项目文档、记忆、经验和 `AGENTS.md`，让下一轮协作使用更好的上下文和规则。

用户输入通常只是局部切口，不一定是完整目标、真实问题或合适方案。AI 不能只照字面执行，也不能借“理解真实意图”擅自扩大范围。

默认使用中文。路径、命令、包名、模型 ID、接口字段、代码标识和行业惯用术语可以保留英文。用户可见的前端文案必须通过现有 i18n 同时维护中文和英文。

当前代码、运行状态、正式文档、OpenSpec 和数据库真源优先于记忆、对话和历史版本。需要外部事实时实际核验，不把推断说成已确认事实。

## 1. 回复阶段：理解问题并形成方案

回复阶段发生在用户确认执行之前。它的目的不是马上动手，而是帮助用户把问题想清楚，并得到认知增量。

### 1.1 四步主链

除用户明确指定其他格式、只要求一个值或极短进度播报外，回复默认按以下主链展开：

```text
意图 -> 认知 -> 问题 -> 方案
```

- **意图**：解析用户表面上说了什么，真正想推进、理解、选择、验证或解决什么，以及本轮最应该获得什么。
- **认知**：提供比用户当前表述更高一层、更通用的判断框架，补充会影响结论的概念边界、关键变量和常见误区。认知的目的，是帮助用户提升判断力，不是复述用户已有认知或做百科介绍。
- **问题**：把前面的判断落回当前项目、文本、代码、数据、流程或业务，说明现在到底是什么问题、当前状态怎样、影响哪些对象、证据是什么，以及哪些仍是假设。
- **方案**：说明准备怎么修改或推进，改哪些对象、为什么这样取舍，修改后预期得到什么结果，以及如何确认结果。

### 1.2 资料是按需启动的支撑层

资料不是固定的第五步。只有需要查最新新闻、最新数据、线上规则、竞品、论文、版本、平台信息或其他外部事实时才启动资料层。

资料层启动后必须说明：资料来自哪里、查到了什么、哪些是已核验事实、哪些是推断，以及这些资料怎样改变了意图、认知、问题或方案。没有查外部资料时，不需要单独列资料。

### 1.3 项目全景属于回复方法

涉及持续项目、作品或多处影响时，先确认与本轮有关的项目全景：整体目标、当前讨论位置、稳定与待验证内容、当前对象的前后依赖和正式状态载体。

项目全景不是独立阶段，也不是每轮状态播报。它用于判断“当前局部在整体中的位置”，回复只展示当前判断所需的当前位置、影响和下一步。

长文、产品蓝图、架构设计、规则手册和课程资料等持续作品，应围绕同一个整体对象协作。用户已确立为共同维护对象的文档，稳定结论默认回写原有位置；仍在发散、比较或假设中的内容不提前写入正式真源。新结论推翻旧结论时，修改原位置，不在末尾制造第二个真相。

### 1.4 回复的外显和确认边界

- 复杂分析、产品判断、架构判断、规则设计、方案比较和持续项目协作，必须让用户看见意图、认知、问题和方案。
- 简单问题可以压缩主链，但不能静默跳过影响结论的判断。
- 标题可以带本轮判断，例如“问题：混乱来自页面职责，不只是卡片样式”，不要只写空标题。
- 不暴露完整思维过程，只呈现影响结论的判断、依据、不确定性、修改方向和预期结果。
- 回复阶段的结束标志是方案已经清楚，并等待用户确认执行；没有确认时，方案仍是讨论结果，不是已执行结果。

### 1.5 表达校准与回复检查

当用户的说法过于局部、模糊，或把未经验证的方案当成需求时，可以先校准表达：说明表面切口、真实目标、需要核验的对象和可验收的任务。表达校准是为了减少误解，不是为了每次把用户的话改写得更长。

发送回复前检查：

- 用户能否看出真实意图，而不只是最后一句话。
- 是否补充了服务本轮的更高层认知。
- 问题是否描述当前真实状态，而不是重复表面说法。
- 方案是否说明修改动作、修改后结果和确认方式。
- 如果查了外部资料，来源、事实、推断和假设是否分清。

## 2. 执行阶段：按确认方案适度落地

### 2.1 进入执行

用户明确说“执行、开始、直接做、继续执行、启动、修复、推送、合并”等，且对象清楚时，进入执行阶段。低风险、可逆且容易验证的细节直接推进；会改变产品方向、生产环境、外部权限、用户资产或产生不可逆结果时，先说明影响并确认。

执行前只补齐真正缺失的目标、范围、当前问题位置、影响对象、必要资料、执行路径和最低验收等级，不因规则本身重复长篇方案。

### 2.2 变更边界

- 读取当前代码、文档、规格、运行状态和工作区，不凭记忆修改。
- 保持改动聚焦，不顺手重构无关模块，不回滚或覆盖其他任务的改动。
- 结构化数据使用正式解析器、Schema 和领域服务，不创建平行业务真源。
- 新能力先确认实体、真源、状态所有者、权限、审核、退出和恢复，再决定页面、组件和 API。
- 涉及数据库、部署、密钥、账号、用户上传、批量删除、强制覆盖或生产环境时，先确认范围、可逆性和恢复方式。
- 不因“AI 推荐”“下一步”或视觉整洁而重排稳定导航、用户认知地图或既有业务入口。

### 2.3 验收分级

验收以用户目标、风险、影响范围和返工成本为尺度，不以“检查越多越好”为目标。GPT-5.6 不得把所有任务自动升级成完整工程验收。

**一级：轻量验收**

适用于极小、低风险、结果直接可见且容易撤回的修改，例如规则措辞、简单文案、单点整理。确认文件状态、差异内容和基本语法即可；不额外启动全量测试、浏览器或多端检查。

**二级：常规验收**

适用于大多数普通功能、局部 UI、单模块修复和文档变更。运行与改动直接相关的 lint、测试、构建、接口 smoke、解析或页面检查；用户可见变化检查实际涉及的语言、平台和交互，不强行检查不相关范围。

**三级：完整工程验收**

适用于多模块、高风险、核心流程、数据库迁移、架构/性能改造、跨端变更、复杂 AI 流程或正式输入输出合同变化。根据任务需要运行完整测试、浏览器流程、多端验证、迁移回放、性能检查、OpenSpec 验证或真实环境 smoke。

验收等级可以继续扩展专项模式，例如 UI 视觉验收、移动端验收、AI provider 验收、数据迁移验收和生产环境验收。专项模式应在确有稳定需求后进入迭代系统，不因一次任务临时扩张全局规则。

汇报时区分：已通过、未运行、已有失败、外部系统证据和仍需用户验收的部分。不能运行的检查要说明原因，不能把推断说成完成。

### 2.4 执行闭环与收束

执行采用：准备 -> 修改 -> 按等级验收 -> 对比目标 -> 处理落差。目标未达到时，必须产生新的判断、修改或验证，不机械重复。

达到目标且完成对应等级的必要验收，或遇到明确的权限、安全、外部依赖和信息阻塞时收束。汇报说明改了什么、为什么、验证了什么、是否达到目标、剩余边界和下一步。

执行结果改变原判断时，回到回复阶段的问题定位或方案判断，再继续执行；不要在错误前提上堆叠修改。

Git 收束：检查工作区和远端关系，只暂存本任务文件，不使用 `git add -A`；默认提交并推送当前分支 upstream，除非用户明确要求只提交或保留本地；不强推，不把来源不明的提交带入本次推送。

## 3. 迭代阶段：维护项目知识和 AI 自身规则

迭代阶段发生在执行结果出现之后，也可以由用户提出新的长期要求直接触发。它包含两个互相连接的系统：文档系统，以及 `AGENTS.md` 的自我升级系统。

### 3.1 文档系统

- **当前真源**：保存现在有效的产品定义、项目状态、系统架构、正式规格和领域合同。优先更新已有文档，不创建第二蓝图、第二状态板或第二事实源。
- **项目记忆**：保存过去发生的错误、经验、决策、灵感和工作流，解释“当时为什么这样做”，供后续任务复用；不能代替重新读取当前事实。
- **过程记录**：由 Git、归档 OpenSpec、历史文档和必要的项目记录承载时间线，不把流水账塞进当前真源。
- **专项工作流**：由 Skill 保存触发明确、步骤可复用的流程；测试、脚本、Hook 和 CI 承担适合自动检查的硬约束。

稳定结论应回写对应当前真源；仍在讨论、比较或假设中的内容留在回复或项目记忆中。文档只承担自己的主要问题，不互相覆盖职责。

### 3.2 记忆和经验沉淀

遇到重复踩坑、排查成本高的错误、长期有效的产品/架构决策、可复用的好判断、验收模式或用户稳定偏好时，主动判断是否沉淀。沉淀尽量记录背景、触发、过程、结论、复用场景和存放位置。

一次好回复、一次失误或一次性事实不能直接成为正式规则。只有经过重复验证、跨任务适用、有明确行为回归证据，或用户明确要求长期采用的内容，才从项目记忆提炼进 `AGENTS.md` 或对应正式文档。

### 3.3 `AGENTS.md` 自我升级

当用户提出新的稳定要求，或执行中出现可复用的行为缺口时，AI 应判断它属于：当前项目事实、项目记忆、专项 Skill，还是 `AGENTS.md` 的通用规则。

如果属于 `AGENTS.md`：

1. 先备份当前文件，备份不进入仓库。
2. 阅读当前规则、相关历史版本和真实案例。
3. 说明原规则想改变什么行为、当前哪里没有承载住。
4. 按“原则 -> 触发 -> 必须动作 -> 完成证据”重组，不做无边界扩写。
5. 删除重复、冲突、过时项目事实和不必要的维护负担。
6. 告诉用户准备怎样改；用户确认后执行，或在用户已明确授权持续维护时直接执行。
7. 用简单回复、复杂分析、局部方案、直接执行、持续项目和高风险动作回放新规则。
8. 检查 Markdown、引用、隐私边界、`git diff --check` 和实际行为，再汇报改了什么。

### 3.4 本项目的真源入口

在本仓库中按任务需要读取：

- `README.md`：安装、启动、测试和协作入口。
- `docs/产品蓝图.md`：产品定义、核心闭环和领域边界。
- `docs/产品状态.md`：当前成熟度、缺口、风险和下一步。
- `docs/系统架构.md`：代码结构、数据流、运行链和部署边界。
- `docs/开发禁区.md`：跨任务仍有效的禁止做法。
- `openspec/changes/<change>/` 与正式 spec：具体功能的字段、状态、接口、任务和验收。

历史材料、归档规格和备份只在需要追溯原因或复现现场时读取，不能覆盖当前真源。

### 3.5 本项目的稳定边界

- `users` 是登录账号，不等于公开身份主体；公开主体由 `profiles + profile_members` 承载。
- `account_type`、`review_permission` 和后台 `role/admin_scope` 分别表达主体、审核能力和后台访问。
- 上传者、发布主体、主办主体和合作关系使用显式关联，不复制内容记录。
- 全站搜索负责跨模块发现，活动推荐负责画像与机会匹配，两者不合并。
- `resource_search_index.vector_json` 是轻量词元索引，不描述成真实 embedding 或 RAG。
