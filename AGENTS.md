# 拓途浙享项目规则

本文件只保存拓途浙享项目特有的长期经验。通用的五步回复法、执行授权、备份、隐私、验收和 Git 规则遵守上级通用 `AGENTS.md`，这里不再复制一套。

```text
项目规则
├── 一、项目文件：到哪里找当前事实
├── 二、经验维护：什么内容值得写进来
└── 三、项目经验
    ├── 产品定位与真源
    ├── 动手前的判断
    ├── 身份、数据与 AI
    ├── 页面职责与信息架构
    ├── 视觉与交互
    ├── 多端、英文与主题
    ├── 文书与项目材料
    ├── 工程与交付
    ├── 验收与汇报
    └── 未分类经验
```

## 一、项目文件

1. `README.md`：项目简介、安装、启动、测试和基本使用入口。

2. `docs/产品蓝图.md`：产品定位、服务对象、模块关系、长期闭环和正式真源。

3. `docs/产品状态.md`：各模块当前进度、主要缺口、风险和下一步。

4. `docs/技术架构.md`：代码结构、数据库、接口、运行链、多端边界和部署方式。

5. `openspec/specs/`：已经稳定的功能合同、字段、状态和验收场景。

6. `openspec/changes/`：正在实施的复杂变更、设计、任务和验收标准。

7. `docs/事实.md`：已经发生的重要结果、用户反馈和证据。

8. `docs/经验.md`：尚在验证的候选经验；经过多次任务确认后，合并进本文件。

9. `docs/指南/`：发布、采集、打包等可以重复执行的操作方法。

10. `docs/开发禁区.md`：已经验证的工程禁止项和高代价风险，处理相关代码时必须同时读取。

11. `AGENTS.md`：经过多次任务验证，能够直接改变后续行动的项目经验。

具体功能、字段、数据库结构和运行状态，以当前代码、migration、OpenSpec、正式 spec 和真实运行结果为准。

## 二、经验维护

按内容的性质选择落点：

```text
产品定位与长期边界     → docs/产品蓝图.md
当前进度与缺口         → docs/产品状态.md
稳定技术结构           → docs/技术架构.md
具体功能合同           → OpenSpec / 正式 spec
执行结果与用户反馈     → docs/事实.md
尚待验证的方法         → docs/经验.md
稳定、可复用的行动方法 → AGENTS.md
专项操作步骤           → docs/指南/
```

1. 历史对话、旧截图、旧文书和归档方案只提供候选经验。先与当前项目事实核对，再决定是否继续采用。

2. 用户明确提出长期要求，或者相似问题反复出现且原因已经明确时，才把经验写入本文件。一次活动、某张海报的尺寸和临时页面文案不直接升级为规则。

3. 新经验优先合并或修正旧经验，不在不同章节重复。同一句只讲一件事，使用人能直接看懂的语言，避免堆叠近义词和抽象名词。

4. 产品定义写入蓝图，当前进度写入状态，技术事实写入架构，字段与状态机写入 OpenSpec。AGENTS.md 不与这些文件争夺真源。

5. 可以由测试、类型、数据库约束、CI 或正式合同完整保证的细节，不在这里再维护一份。

6. 暂时无法归类的稳定经验放在最后的“未分类经验”。同类内容积累后再建立新分类。

## 三、项目经验

### 3.1 产品定位与真源

1. 拓途浙享是拓浙 AI 生态的数字平台，不等于整个 AI 生态。网站承接信息发现、社区沉淀、项目实践、浙客松赛事、主体展示和后台治理；组织、线下社群、人才培养和产业合作仍有各自的现实载体。

2. 活动、AI 社区、项目广场、浙客松、profile 和后台不是互不相关的栏目。它们共同完成“发现机会—建立连接—参与实践—沉淀成果—持续治理”的产品闭环。

3. 新功能先说明服务谁、解决什么真实问题、把用户带到哪一步。访问量、栏目数、媒体数和炫目的统计不能代替有效连接与后续行动。

4. 社区内容保持统一心智。网页、文档、CLI 和微信采集进入共享的归属、解析、审核和发布链，不为新闻、教程、组队或微信群另建平行内容系统。

5. 项目是正式业务对象，不是帖子或装饰卡片。项目卡、成员、主体、状态、成果和组队关系进入同一项目链；收藏、分享海报和自定义主页只做投影。

6. 活动、赛事、项目、成果、照片和视频通过明确关系连接。文件名、标签、页面分组和静态数组只能辅助展示，不能决定正式归属。

7. 上传者、发布主体、主办方、活动提供方和品牌合作伙伴是不同关系。前台表达和后台权限必须根据正式字段判断，不能从名称或 profile 是否存在直接推断。

### 3.2 动手前的判断

1. 开始分析、修复或发布前，先确认仓库、分支、工作树、正在运行的服务、页面地址、数据库和配置是否属于同一版本。主目录、Codex worktree、服务器和旧对话可能处在不同现场。

2. 判断一项能力是否完成时，沿真实用户路径检查入口、输入、处理、结果、编辑或确认、失败恢复、后台治理和正式数据关系。只有 API、页面外壳或一次成功演示，都不算闭环完成。

3. 修复问题先找原因，再检查本次范围内是否还有同一触发机制造成的同类问题。不能只隐藏眼前现象，也不能因为“全面优化”顺手改动无关模块。

4. 文案、文档、局部样式和单点错误可以快速执行；核心流程、跨模块能力、数据库、权限、API 或复杂 AI 流程使用 OpenSpec。已有 change 能覆盖任务时继续使用，不建立平行方案。

5. 新能力先确认正式实体、真源、状态所有者、权限、审核、失败、退出和恢复方式，再决定页面、组件和 API。

6. 用户已经确认满意或明确要求保留的区域，不得在相邻任务中顺手重做。需要改变时先说明原因、影响和新旧差异。

### 3.3 身份、数据与 AI

1. `users` 承载登录账号，`profiles + profile_members` 承载公开的个人和组织主体。数字名片、组织主页和目录都是 profile 的展示，不再创建一套名片账号或组织副本。

2. `account_type`、`review_permission` 和 `role/admin_scope` 分别表达账号主体、发布审核和后台访问。组织账号不自动是管理员，免审核不自动拥有后台，前端隐藏按钮不能代替后端权限校验。

3. 历史内容归属迁移先形成候选、别名和证据，再由人确认。名称、地点、标题和上传者不能单独决定所有权，也不能通过复制或删除重建来伪造迁移。

4. 全站搜索负责主动查询和跨模块发现，活动推荐负责基于画像的机会判断。两者可以共享模型和遥测，但保留各自的输入、排序、解释、反馈和入口。

5. `resource_search_index.vector_json` 当前是轻量词元表示，不能在页面、注释或材料中宣传成真正的 embedding 或 RAG。能力升级后再同步修改实现、评测和表述。

6. AI 默认产生搜索条件、推荐解释、提取结果、治理建议或迁移候选。正式内容、主体归属、权限和审核仍经过领域服务与用户或管理员确认。

7. 模型无结果、结构非法、证据不足和质量不足必须区分。fallback 只能提供诚实降级，不能把建议伪装成正式事实或把失败显示成成功。

8. 微信登录、文章抓取、解析、审核和正式发布是不同状态。Cookie、二维码现场和一次 Playwright 页面只是采集基础设施，不是内容真源。

9. 数据库结构通过 `ensureCoreSchema`、`runMigrations` 或正式 migration 演进。不能在 controller 或热请求路径临时改表；写操作只有在领域幂等和重复回执明确时才能重试。

### 3.4 页面职责与信息架构

1. 先明确页面服务的对象、主要任务和操作顺序，再决定布局。复用组件可以统一体验，但不能把不同页面的用途混在一起。

2. 每项内容放在真正负责它的页面。品牌故事和生态说明进入 About 或品牌页面，活动行动进入活动页，项目关系进入项目页，治理能力进入后台；不要为了“丰富首页”到处复制同一内容。

3. 导航名称表达用户要去哪里，不复述内部组织结构。新增栏目之前先检查现有页面是否已经承担同一任务，避免同义入口、兄弟模块和重复详情页。

4. 一个页面只突出一个主任务。主要操作靠近它影响的对象，次要信息按需展开；稳定导航、清晰分类和随时返回比层层“下一步”更能给用户掌控感。

5. 合并栏目或迁移路由前，搜索目标页所有 query、hash、深链和旧入口消费者。已经被兄弟模块使用的参数不能直接复用。

6. 前台存在的正式能力必须有后台治理路径。页面按钮看起来能够点击时必须真实可用；暂不可用时明确禁用、原因和恢复方式。

### 3.5 视觉与交互

1. 项目的视觉目标是简洁、现代、清楚和高效。先保留对用户有用的信息，再用排版、字号、留白和位置建立层级；不要先画很多容器，再往里面填内容。

2. 删除无意义的大标题、重复说明、装饰标签和无用统计。正文与主要操作尽早进入首屏；能够放在一行的搜索、筛选、状态和视图切换优先合并。

3. 简洁不等于空，也不等于删除能力。重要数据要有足够信息量，但应使用与信息匹配的列表、表格、时间线、图表或详情层，不靠平铺卡片和长段解释堆积。

4. 页面不要替用户做决定。文案说明事实、状态和可选操作，不用夸张口号、强迫式“下一步”或大量解释性语言指挥用户。

5. 多步骤、长内容或持续操作使用完整工作区；弹窗、抽屉和侧栏只承载短流程、补充信息和就近操作。内容很少或为空时，布局随状态收拢，不保留巨大的空壳。

6. 界面要显示真实状态：正在处理什么、进展到哪里、哪些内容发生变化、为什么失败、怎样继续。技术细节可以按需展开，不能抢占主视线。

7. 当前已经成立的品牌色、渐变、动效、白天和黑夜体系是设计基线。先复用共享 token 和组件，再修复真实冲突；不能根据单张截图另起一套页面补丁。

8. 浮层闪烁、裁切和遮挡同时检查 portal、祖先 `transform/overflow`、堆叠上下文、`pointer-events`、视口边界和滚动锁，不能只增加 `z-index`。

### 3.6 多端、英文与主题

1. 拓途浙享包含 Web、PWA、移动端封装、微信场景和桌面页面，但不是每次修改都要验收所有平台。先根据任务和正式产品范围确定受影响的端、语言与主题，只检查真实影响范围。

2. 同一能力在不同平台保持一个用户入口。上传、分享、登录、文件选择和打开页面由运行时选择网页 API、小程序桥或原生能力，不在界面上并排放多个平台按钮。

3. 移动端不是桌面端的机械缩小版。底部导航只保留高频入口，次级页面进入 More 或 Profile；筛选、排序、菜单和抽屉必须有稳定、可见、可返回的打开方式。

4. 移动浮层同时检查 `100dvh`、安全区、底部导航、返回键、遮罩、scroll lock 和 portal。取消分屏时还要检查外层高度、内部对齐和横向轨道。

5. 当前前端维护中文和英文。修改用户可见文案时同步词典，并检查本次涉及页面是否出现原始 key、异常字符或明显溢出；不因局部改动开展无关的全站翻译审计。

6. 白天和黑夜问题先检查 theme token、背景、canvas、fallback、overlay 和 active state 的完整链路，不用逐页颜色覆盖制造新的主题分支。

### 3.7 文书与项目材料

1. 项目文书先讲清“这是什么、为什么存在、整体怎样运行”，再展开业务、成果和下一步。不要为了套模板打乱叙述顺序，也不要把理念、业务、组织和发展方向混成同一层。

2. 写作追求简洁、具体和有信息量。保留已经确认的关键词与事实，减少官话、叠加词、连续抽象名词和反复的“不是……而是……”句式。

3. 结构为内容服务。适合并成完整段落的内容不要切成碎片；真正存在并列、对照、流程或层级时，再使用编号、表格或图。不能为了显得有结构把所有句子平铺成项目符号。

4. 精简时保留关键事实、因果关系和逻辑闭环。删掉重复和空话，不把有价值的具体内容一起压掉；每次改写都能说清保留了什么语义。

5. 流程图和关系图只在能明显帮助理解时使用。节点、方向、层级和字体必须可读，图中文字不能小到只能放大查看，也不能用歪斜的装饰线代替清楚关系。

6. 修改已有 DOCX 的少量文字时，优先在原文件中替换并保留版式，不重新生成整份文档。只有用户要求 PDF 或交付场景明确需要时才导出，并让 DOCX 成为可继续编辑的真源。

7. 比较长文档时优先提取完整文字、标题和结构；只有需要判断排版、字体、图表或分页时才渲染页面。截图不能代替全文内容核对。

8. 商业计划、内部文书、融资材料、会议记录、成员信息和本地导出文件不得进入公开 Git。项目需要展示的公开版本必须与内部原件分开管理。

### 3.8 工程与交付

1. `docs/开发禁区.md` 保存详细工程护栏。涉及身份、权限、内容归属、搜索、AI、采集、数据库、多端、缓存或部署时，必须读取对应章节，不能只依赖本文件摘要。

2. 根 Service Worker 不能接管 `/lingzhi/`、`/code/` 等同域子应用。新增子应用同时检查 Caddy location、导航回退、runtime cache 和已经被旧 Service Worker 控制的标签页。

3. 当前生产代理是 Caddy。发布前执行配置验证，发布后确认 reload、进程、首页、子路由、API 和静态资源；不能把已经退役的 Nginx 状态当成生产故障。

4. 启动任务要确认进程持续、HTTP 成功、后端健康和页面可见。PID、一次请求或前台工具会话不能单独证明服务稳定运行。

5. 数据库、WAL、备份、上传目录、密钥、用户数据、截图、办公文档、商业材料、会话记录和本地生成产物不得进入公开仓库。

6. Git 只暂存本任务文件，不使用 `git add -A`，不覆盖用户或其他任务已有改动。分支分叉、远端冲突或存在发布风险时停止，不强推。

7. 批量删除、迁移、归属修正和文档体系重整先建立仓库外备份并验证可恢复性。清理时逐项说明删除理由，不能把“界面不要”理解成“能力和数据都可以删”。

### 3.9 验收与汇报

采用能够证明目标完成的最小充分验收：

1. Markdown、规则和小范围文案检查内容差异、结构、链接和 `git diff --check`，不启动完整项目或截图。

2. 已有 DOCX 只改文字时，核对目标文字、原有结构和文件可打开；普通文档结构调整检查标题、内容和链接，只有排版发生变化时才渲染。

3. 普通代码改动运行直接相关的测试、lint、构建或页面验证；跨模块、核心流程、数据库、权限和复杂 AI 变化才扩大范围。

4. 用户可见变化检查本次涉及的真实页面、状态、语言、主题和设备。截图只说明页面出现了什么，原因仍要由 DOM、代码、接口和数据确认。

5. AI 变化区分 mock、golden、真实 provider、压力、错误恢复和长期效果。构建通过不能代替真实模型、浏览器、微信、Android、iOS 或生产环境证据。

6. 数据库变化检查 migration、幂等、备份、回退和真实 schema；启动成功不能代替数据正确性。

7. 证据已经足够时停止继续验收。汇报时分清已通过、未运行、已有失败、外部系统证据和仍需用户确认的部分。

### 3.10 未分类经验

本节只保存暂时无法归类的稳定经验，并始终放在最后。当前暂无。
