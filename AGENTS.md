# 拓浙 AI 生态数字平台项目规则

```text
项目 AGENTS.md
├── 一、文件体系
│   ├── 当前真源
│   ├── 专项与历史资料
│   └── 读取与回写
└── 二、项目规则与沉淀策略
    ├── 沉淀与迭代
    ├── 项目边界
    ├── 执行规则
    ├── 验收规则
    ├── 前端与多端规则
    └── 未分类规则（始终放在最后）
```

## 一、文件体系

### 1.1 当前真源

1. `README.md`：项目简介、安装、启动、测试和基本使用入口。

2. `docs/产品蓝图.md`：产品定位、服务对象、核心闭环、长期模块和产品边界。

3. `docs/产品状态.md`：当前阶段、模块进度、主要缺口、风险和下一步。

4. `docs/技术架构.md`：代码结构、数据真源、运行链、部署方式和技术边界。

5. `AGENTS.md`：本项目的长期协作规则、项目边界、执行方法和验收案例。

6. `openspec/changes/<change>/`：高影响任务当前采用的方案、任务和验收合同；`openspec/specs/` 与正式 spec 保存稳定功能合同。

### 1.2 专项与历史资料

1. `PRODUCT.md` 与 `DESIGN.md`：供特定产品或设计工具读取的当前投影；内容冲突时回到产品文档、代码和运行结果核验。

2. `docs/指南/`：发布、采集、打包和移动端验收等专项操作资料；只在对应任务中读取，不承担项目状态或长期规则。

3. `docs/归档/`：已经退役或不再作为当前施工入口的历史材料；只用于追溯与定向提取，不能覆盖当前真源。

4. `docs/事实.md`：本地记录真实发生的任务、动作、反馈、结果和证据，不进入公开 Git。

### 1.3 读取与回写

1. 根据任务读取相关真源，不机械读取全部文档；专项资料和历史材料只在需要时使用。

2. 文档与当前代码、数据库或运行结果不一致时，先核验当前事实，再修正文档。

3. 稳定结论写回原有真源，不创建平行版本；具体分流遵循下方“沉淀与迭代”规则。

## 二、项目规则与沉淀策略

### 2.0 沉淀与迭代

```text
真实变化
├── 项目发生稳定变化 → 更新对应项目文档或 OpenSpec
└── 执行结果或用户反馈 → 记录到 docs/事实.md
    └── 形成稳定、可复用的行动方法 → 写入 AGENTS.md
```

1. 更新项目文档：产品定位和长期边界写入产品蓝图，阶段与缺口写入产品状态，代码、数据和运行链写入技术架构，具体功能合同写入 OpenSpec。

2. 记录事实：重要任务结果、用户明确反馈、高代价失败和新的稳定要求写入 `docs/事实.md`。只记录时间、背景、动作、结果、反馈和证据，不记录普通过程与尚未发生的判断。

3. 提炼规则：用户明确提出长期要求，或者相似事实反复出现且原因已经明确时，提炼成能够改变行动的规则，直接写入 `AGENTS.md`，不另建 `docs/经验.md`。

4. 归类规则：新规则优先写入已有分类；暂时无法归类时写入最后的“未分类规则”。同类规则积累成稳定主题后，将其整理为新分类，并重新生成最后的“未分类规则”。

5. 保持轻量：重要任务结束后自动检查事实与规则是否需要记录、合并或归类。新规则优先替换或合并旧规则；能够由测试、lint、CI 或正式合同保证的约束，不重复写入 `AGENTS.md`。修改核心项目边界或通用规则前仍需用户确认。

### 2.1 项目边界

1. 产品定位：拓浙 AI 生态数字平台连接学生、组织、学院、企业、真实 AI 需求与产业机会，是生态的数字底座和公共入口，不是活动聚合站、新闻站或数字艺术展示站。

2. 品牌名称：公开品牌优先使用“拓浙 AI 生态”；只有需要区分数字平台、线下生态、商业交付或学校职责时，才使用“拓途浙享”说明数字载体。

3. 业务闭环：活动、AI 社区、项目广场、浙客松、profile、搜索推荐和后台属于同一个生态转化系统，不能按孤立栏目分别建设。

4. 内容与项目：AI 社区的网页、文档、CLI 和微信采集进入共享审核与归属链；项目、活动、主体、成果和合作关系使用明确实体与关联，帖子、收藏、海报和媒体文件不能代替这些业务对象。

5. 身份与权限：`users` 是登录账号，`profiles + profile_members` 承载个人和组织主体；`account_type`、`review_permission` 和 `role/admin_scope` 分别表达主体、审核能力和后台访问权限。

6. 搜索与 AI：全站搜索负责跨模块查询，活动推荐负责画像驱动的机会判断，两者不合并；`resource_search_index.vector_json` 是轻量词元表示，不宣称为真实 embedding 或 RAG；AI 只产生建议、条件或候选，不直接写正式业务事实。

7. 数据：Schema 由核心建表与正式 migration 决定，不在 controller 或热请求路径临时改表。

8. 多端：Web/PWA 是产品和代码主真源；微信、Android 和 iOS 复用同一产品能力与用户入口。

9. 部署：`master` 推送触发 GitHub Actions，生产入口是 Caddy，不恢复已经退役的 Nginx 步骤。

### 2.2 执行规则

1. 文案、文档、局部样式、单点错误和不改变正式合同的小范围修改采用快速执行；核心业务链、跨模块改造、数据库迁移、身份权限、审核归属、API、状态合同和多端交付使用 OpenSpec。

2. 已有 OpenSpec 覆盖当前任务时，继续原有 change，不重建平行方案；历史 OpenSpec 只用于追溯，不覆盖当前代码和正式 spec。

3. 新能力先确定业务实体、正式真源、状态所有者、权限、审核、退出和恢复，再设计页面、组件和 API。

4. 新页面和新入口复用已有主体、内容、项目、审核和权限链，不复制业务事实。

5. 数据库变化进入核心 schema 或正式 migration，并考虑幂等、备份、回退和失败可见性。

6. 上传、报名、收藏、审核和迁移不默认重试；只有领域幂等与重复回执明确时才能重试写操作。

7. 用户可见文案通过现有 i18n 同时维护中文和英文。

8. 多端保持一个用户入口，再根据 Web、PWA、微信、Android 或 iOS 的实际运行状态采用对应桥接和降级链。

9. 只暂存本任务文件，提交前检查暂存范围和 `git diff --cached --check`。用户未要求只保留本地时，任务闭环后安全对齐并推送 `origin/master`；不得覆盖远端新提交。

10. 登录 token 默认使用 `sessionStorage`；只有用户明确选择持久登录时才使用 `localStorage`。登录、登出、微信回执和多端桥接复用同一 storage helper，不在 URL、日志、公开文档或静态配置中暴露凭据。

11. 修改 locale 使用 UTF-8 和结构化方式，完成后解析 JSON 并检查异常字符；代码图谱只用于导航，最终判断回到源码、diff、测试和真实运行结果。

### 2.3 验收规则

继承通用文件的“最小充分验收”。以下是当前项目已经明确的专项案例，每次只执行与改动直接相关的检查。

1. `AGENTS.md` 或普通 Markdown：检查目标差异、标题结构和 `git diff --check`，不启动站点、浏览器或截图。

2. 产品蓝图、产品状态或技术架构：检查文档职责、链接和对应的代码或状态证据，不运行无关工程测试。

3. Locale 或用户文案：检查中英文对应项、JSON 解析和异常编码；只有显示逻辑受到影响时才打开页面。

4. 局部前端逻辑：运行受影响文件的 ESLint 或相关测试；模块、路由或打包边界改变时再补充构建。

5. UI 外观或布局：打开真实受影响页面，只检查相关断点和主题，只对受影响的页面和设备截图。

6. 路由、动画、深链或 PWA 缓存：验证 URL、页面可见性、返回链、query/hash 和实际 Service Worker 导航边界。

7. 后端 API、权限或 Schema：运行相关领域测试，核对后端权限、migration 和失败合同。

8. AI 能力：根据改动选择结构检查、golden、live、压力或恢复测试；Mock 和 golden 不代表真实 provider 已通过。

9. 微信采集：分别报告登录、抓取、解析、审核和发布状态，不用登录健康代替采集成功。

10. 微信、Android 或 iOS：只验证受影响平台；发布级结论必须有对应工具、模拟器或真机证据。

11. OpenSpec：验证对应 change 和相关测试；只有正式 spec 或多个 change 受到影响时才运行全量 OpenSpec 验证。

12. 生产发布：确认 GitHub Actions、Caddy 和服务状态，再对受影响的 HTTPS 或业务链进行 smoke。

汇报时分清已经通过、没有运行、原本存在的失败、外部系统证据和仍需用户或真实环境确认的部分。

### 2.4 前端与多端规则

1. 路由切换以导航正确为先；出现 URL 已变化但旧页面残留时，优先检查路由级退出动画和 `AnimatePresence`，不让旧页面动画阻塞新页面。

2. 新增或迁移 query、hash 与深链参数前，先检查目标页面的全部消费者；兼容重定向必须验证不会误开兄弟模块，并保留正确返回链。

3. 复制与分享提供 Web API、传统复制或可选择文本的有界降级；权限失败时给用户可操作内容，不只显示失败提示。

4. 移动弹层同时检查 portal、祖先 transform、`100dvh`、scroll lock、安全区、返回行为和底部导航，不用单独增加 `z-index` 掩盖结构问题。

5. 主题问题先对照共享语义 token、DOM 和日夜分支，再做代表性视觉验收；不以逐页颜色补丁扩张主题系统。

6. 根 PWA 不得接管 `/lingzhi/`、`/code/` 等同域独立应用；新增子应用同时检查 Caddy 路由、Service Worker denylist、runtime cache 和旧 SW 控制标签页的导航。

7. 多平台保持同一个可见用户入口，运行时再选择 Web API、WebView 或原生桥；移动端次级操作必须有稳定入口，并验证打开、关闭、返回、遮罩和底栏遮挡。

8. 新增动效优先复用共享 motion token 和 `prefers-reduced-motion`；尺寸、布局流和高成本模糊不作为默认动画，只有新增交互语义时才扩展共享系统。

### 2.5 未分类规则

本节保存已经稳定、但暂时不适合现有分类的项目规则，始终放在第二章最后。当前暂无。
