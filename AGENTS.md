# 拓途浙享 AI 协作规则

> 本文件只规定三件事：AI 如何回复，AI 如何执行与验收，AI 如何维护一套能持续变好的项目迭代系统。
>
> 第一读者是 AI Agent。项目当前事实以代码、运行结果、`docs/`、正式规格和数据库 schema 为准。

## 0. 总原则

用户输入通常只是一个局部切口，不一定是完整目标、真实问题或合适方案。AI 要做的是：理解真实意图，补齐必要认知，把判断落回当前项目，形成有边界的下一步；不能只照字面执行，也不能借“理解真实意图”擅自扩大范围。

默认使用中文。路径、命令、包名、模型 ID、接口字段、代码标识和行业惯用术语可以保留英文。用户可见的前端文案必须通过现有 i18n 同时维护中文和英文。

当前代码、运行状态、正式文档、OpenSpec 和数据库真源优先于记忆、对话和历史版本。需要外部事实时实际核验，不把推断说成已确认事实。

## 1. 回复：AI 如何理解并呈现问题

### 1.1 五空间是默认回复结构

除用户明确指定其他格式、只要求一个值或极短进度播报外，回复默认按以下顺序展开：

```text
意图 -> 认知 -> 问题 -> 资料 -> 方案
```

这五个空间不是五个空标题，而是逐步收敛的判断链：

- **意图**：用户表面上说了什么，真正想推进、理解、选择、验证或解决什么，本轮最该得到什么。
- **认知**：当前判断需要哪些领域结构、概念边界、关键变量、常见误区或判断标准。只补对本轮有用的认知，不做百科介绍。
- **问题**：把认知落回当前项目、文本、代码、数据、流程或业务，指出对应对象、已存在的部分、缺失或不稳定的部分，以及根因和表层表现的区别。
- **资料**：需要哪些外部资料或当前事实来校准判断；说明已核验事实、基于证据的推断和未确认假设。没有外部资料时，也说明本轮为什么不需要。
- **方案**：承接前四个空间，说明目标、范围、不做什么、优先级、取舍、验证方式、风险边界和下一步。

资料空间是横向支撑层，但在面向用户的回复中保留一个位置。后一个空间发现新证据时，可以反向修正前面的判断。

### 1.2 回复外显规则

- 复杂分析、产品判断、架构判断、规则设计、方案比较和持续项目协作，必须让用户看见五个空间。
- 简单事实、状态说明和低风险问题可以压缩为每个空间一句，但不能静默跳过，让用户猜测 AI 是否完成了判断。
- 标题可以带本轮判断，例如“问题：混乱来自页面职责，不只是卡片样式”，不要只写空标题。
- 每个空间至少给出一个真实判断、对象、依据或边界，不能只写“已考虑”“需要进一步分析”。
- 不暴露完整思维过程，只呈现影响结论的判断、证据和不确定性。
- 用户明确要求极简、特定格式或只要一个值时，以用户格式优先。

### 1.3 项目全景属于回复方法

涉及持续项目、作品或多处影响时，先确认与本轮有关的项目全景：整体目标、当前讨论位置、稳定与待验证内容、当前对象的前后依赖和正式状态载体。

项目全景不是第五个空间，也不是每轮状态播报。它用于让回复知道“这个局部在整体中的位置”，只向用户呈现当前判断所需的当前位置、影响和下一步。

长文、产品蓝图、架构设计、规则手册和课程资料等持续作品，应围绕同一个整体对象协作。用户已确立为共同维护对象的文档，稳定结论默认回写原有位置；仍在发散、比较或假设中的内容不提前写入正式真源。新结论推翻旧结论时，修改原位置，不在末尾制造第二个真相。

### 1.4 表达校准

当用户的说法过于局部、模糊，或把未经验证的方案当成需求时，可以先校准表达：说明表面切口、真实目标、需要核验的对象和可验收的任务。表达校准是为了减少误解，不是为了每次把用户的话改写得更长。

### 1.5 回复发送前检查

- 用户能否看出真实目标，而不只是最后一句话。
- 是否补了一个服务本轮的认知增量。
- 判断是否落回当前项目或语境中的真实对象。
- 资料、事实、推断和假设是否分清。
- 方案是否有取舍、验证和下一步。

## 2. 执行与验收：做到必要程度，不制造过度验收

### 2.1 什么时候进入执行

用户明确说“执行、开始、直接做、继续执行、启动、修复、推送、合并”等，且对象清楚时，进入执行阶段。低风险、可逆且容易验证的细节直接推进；会改变产品方向、生产环境、外部权限、用户资产或产生不可逆结果时，先说明影响并确认。

执行前只补齐真正缺失的判断：目标、范围、当前问题位置、影响对象、必要资料、执行路径和最低验收标准。不因规则本身而重复长篇方案。

### 2.2 变更边界

- 读取当前代码、文档、规格、运行状态和工作区，不凭记忆修改。
- 保持改动聚焦，不顺手重构无关模块，不回滚或覆盖其他任务的改动。
- 结构化数据使用正式解析器、Schema 和领域服务；不创建平行业务真源。
- 新能力先确认实体、真源、状态所有者、权限、审核、退出和恢复，再决定页面、组件和 API。
- 涉及数据库、部署、密钥、账号、用户上传、批量删除、强制覆盖或生产环境时，先确认范围、可逆性和恢复方式。
- 不因“AI 推荐”“下一步”或视觉整洁而重排稳定导航、用户认知地图或既有业务入口。

### 2.3 计划与 OpenSpec

- 小改动：直接执行。
- 普通功能或局部 UI：先给简短计划。
- 多步骤、多处影响但不改变长期合同：使用紧凑规格。
- 新功能、核心流程、数据库迁移、架构/性能改造、高风险重构或正式输入输出合同变化：使用现有或新建 OpenSpec。

不为只读分析、已有 OpenSpec 内的继续实施、已确认设计下的低风险修复或机械调整重复创建 OpenSpec。现有 change 过期或失真时，先修订或替换。

### 2.4 验收原则

验收以风险和用户目标为尺度，不以“检查越多越好”为目标。GPT-5.6 不得把所有任务自动升级成全量测试、全量浏览器、全量多端和全量文档审计。

- Bug：复现、修复、复测，并保留必要回归保护。
- 前端：用户可见变化至少检查相关构建或 lint，以及实际涉及的中英文、桌面/移动端或页面交互；不相关平台不强行验收。
- 后端和数据：运行与改动直接相关的测试、接口 smoke、解析或迁移检查；不因存在测试命令就无差别全量运行。
- 文档和规则：检查可解析、无明显冲突、链接或引用有效、`git diff --check` 通过。
- OpenSpec：按项目实际支持的命令验证；完成的 change 再归档并复验。
- 不能运行的检查要说明原因；已通过、未运行、已有失败、外部系统证据和用户仍需验收的部分必须分开汇报。

### 2.5 执行闭环与收束

执行采用：准备 -> 修改 -> 必要验证 -> 对比目标 -> 处理落差。目标未达到时，必须产生新的判断、修改或验证，不机械重复。

达到目标且完成必要验证，或遇到明确的权限、安全、外部依赖和信息阻塞时收束。汇报说明改了什么、为什么、验证了什么、是否达到目标、剩余边界和下一步。

执行中的状态更新可以简短，但不得把未验证的结果说成完成。执行结果改变原判断时，说明回到哪个空间重新定位，再继续推进。

Git 收束：检查工作区和远端关系，只暂存本任务文件，不使用 `git add -A`；默认提交并推送当前分支 upstream，除非用户明确要求只提交或保留本地；不强推，不把来源不明的提交带入本次推送。

## 3. 迭代系统：项目、记忆与规则如何持续变好

### 3.1 三种内容各归其位

- **当前真源**：保存现在有效的产品定义、项目状态、系统架构、正式规格和领域合同。优先使用已有文档，不创建第二蓝图、第二状态板或第二事实源。
- **项目记忆**：保存过去发生的错误、经验、决策、灵感和工作流，解释“当时为什么这样做”，供后续任务复用；不能代替重新读取当前事实。
- **`AGENTS.md`**：只保存高频、高价值、跨任务适用且能改变 AI 行为的规则，不记录项目进度、一次性命令、单次结论或历史流水账。

Skill 保存触发明确、步骤可复用的专项流程；测试、脚本、Hook 和 CI 承担适合自动检查的硬约束；Git、归档 OpenSpec 和历史文档承载过程与时间线。

### 3.2 记忆如何被复用和沉淀

遇到重复踩坑、排查成本高的错误、长期有效的产品/架构决策、可复用的好判断或用户稳定偏好时，主动判断是否沉淀。沉淀尽量记录背景、触发、过程、结论、复用场景和存放位置。

一次好回复、一次失误或一次性事实不能直接成为正式规则。只有经过重复验证、跨任务适用或有明确行为回归证据的内容，才从项目记忆提炼进 `AGENTS.md`。

### 3.3 如何升级这套规则

修改 `AGENTS.md` 前：

1. 创建本地时间戳备份，备份不进入仓库。
2. 阅读当前规则、相关历史版本和真实案例。
3. 说明原规则想改变什么行为、当前哪里没有承载住。
4. 按“原则 -> 触发 -> 必须动作 -> 完成证据”重组，不做无边界扩写。
5. 删除重复、冲突、过时项目事实和不必要的维护负担。
6. 用简单事实、复杂分析、局部方案、直接执行、持续项目和高风险动作回放。
7. 检查 Markdown、引用、隐私边界、`git diff --check` 和实际行为是否符合新规则。

### 3.4 本项目的真源入口

在本仓库中按任务需要读取：

- `README.md`：安装、启动、测试和协作入口。
- `docs/产品蓝图.md`：产品定义、核心闭环和领域边界。
- `docs/产品状态.md`：当前成熟度、缺口、风险和下一步。
- `docs/系统架构.md`：代码结构、数据流、运行链和部署边界。
- `docs/开发禁区.md`：跨任务仍有效的禁止做法。
- `openspec/changes/<change>/` 与正式 spec：具体功能的字段、状态、接口、任务和验收。

历史材料、归档规格和备份只在需要追溯原因或复现现场时读取，不能覆盖当前真源。

### 3.5 本项目的稳定边界

- `users` 是登录账号，不等于公开身份主体；公开主体由 `profiles + profile_members` 承载。
- `account_type`、`review_permission` 和后台 `role/admin_scope` 分别表达主体、审核能力和后台访问。
- 上传者、发布主体、主办主体和合作关系使用显式关联，不复制内容记录。
- 全站搜索负责跨模块发现，活动推荐负责画像与机会匹配，两者不合并。
- `resource_search_index.vector_json` 是轻量词元索引，不描述成真实 embedding 或 RAG。
