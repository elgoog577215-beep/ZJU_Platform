# 拓途浙享 AI 协作规则

> 第一读者：AI Agent。本文规定 AI 怎样理解用户、判断问题、控制范围、修改项目、验证结果和收束交付。
>
> 人类开发者从 [README.md](./README.md) 进入项目；当前产品与工程事实由 `docs/` 中的中文文档承载。

## 0. 总纲

### 0.1 核心目的

用户输入通常是有价值的局部切口，不一定是问题全貌、最终任务说明或最合适的实施路径。AI 既不能只完成字面任务，也不能借“理解真实意图”擅自扩大范围。

每轮协作围绕同一条主线：

```text
理解用户真正想推进、理解、验证或解决什么
-> 读取当前项目、文件、代码、数据和运行事实
-> 补齐当前判断真正需要的认知与外部资料
-> 定位局部问题在产品、代码和业务全景中的位置
-> 形成有取舍、有边界、可验证的方案
-> 在授权范围内执行并验证真实结果
-> 更新受影响的当前真源，或说明为什么不更新
```

本文既是协作判断规则，也是面向用户的回复协议。五空间不是只在内部完成的思考步骤：除用户明确指定其他格式、纯单值事实或正在执行中的极短状态播报外，回复必须按“意图、认知、问题、资料、方案”的顺序让用户看见本轮判断。简单问题可以压缩每个空间，但不能静默省略或让用户猜测本轮是否经过这些判断。

### 0.2 语言与用户文案

- 默认使用中文回复、写项目文档、任务、方案、风险和验收说明。
- 路径、命令、包名、模型 ID、接口字段、代码标识和行业惯用术语可以保留英文。
- 当前运行前端只启用中文和英文；用户可见文案必须同时维护中文和英文。
- 组件通过当前 i18n 入口读取文案，不只在组件、静态数组、fallback、SEO 或无障碍属性中写死中文。
- 当前前端词典位于 `public/locales/zh/translation.json` 与 `public/locales/en/translation.json`。
- 修改 locale 后必须 `JSON.parse`，并检查中文问号乱码、英文模式中文残留、原始 key、遮挡和布局破坏。

### 0.3 事实、项目适配与隐私

当前代码、运行结果、数据库 schema、正式 OpenSpec 和当前文档优先于记忆、对话和历史材料。发生冲突时先核验，再更新对应真源。

本规则的通用部分可以跨项目复用，但执行时必须先适配当前仓库：

- 识别当前项目的真实结构、技术栈、默认分支、文档位置、测试方式和既有约定。
- 本文里的路径、工具和流程只对本项目当前事实有效；同步到其他项目时，不得硬套、硬建或假装同一机制存在。
- 多仓库或跨目录任务中，每个仓库分别检查状态、范围、验证和 Git 收束，不把一个仓库的假设直接套到另一个仓库。

仓库提交不得包含商业计划书、内部文书、用户上传内容、数据库、密钥、个人信息、会话记录、AI memory 和本地生成产物。项目文档只能引用适合进入仓库的产品与工程事实。

## 1. 开始任务时读什么

### 1.1 默认入口

先确认仓库、分支和工作区，再按任务需要读取：

1. [README.md](./README.md)：安装、启动、测试和人类协作入口。
2. [产品蓝图](./docs/产品蓝图.md)：产品定义、核心闭环、领域边界和长期顺序。
3. [产品状态](./docs/产品状态.md)：当前成熟度、活动 OpenSpec、缺口和主攻位置。
4. [系统架构](./docs/系统架构.md)：当前代码结构、领域真源、运行链和部署边界。
5. [开发禁区](./docs/开发禁区.md)：已经确认不能采用的产品和工程做法。
6. 当前任务对应的 `openspec/changes/<change>/`、正式 spec 或必要的局部设计。

不要每轮机械读取全部文档。简单任务只读直接相关对象；跨模块、高影响、产品、架构或部署任务再补齐蓝图、状态和架构。

取证遵循“最小充分证据”：先定位入口、真源和最可能影响结论的对象，再读取相关片段；当已有证据足以支持关键判断、区分事实与假设并形成下一步时停止扩读。只有出现矛盾、缺口、跨模块影响或高风险边界时才继续扩大范围，不因文件相关、工具可用或希望显得全面就扫描全部匹配项。

### 1.2 历史与专项材料

- [项目决策记录](./docs/归档/项目决策记录.md)、`docs/指南/`、`docs/归档/` 和 `openspec/changes/archive/` 都是按需读取材料。
- 只有需要追溯过去原因、执行专项流程或复现特定历史现场时才读取。
- 历史文件中的“当前”“下一步”、测试数量、旧路径和旧架构不具备规范效力。
- 当前结论必须回到蓝图、状态、架构、开发禁区、正式 OpenSpec、代码和测试重新确认。
- 旧 `docs/ai-memory/`、规则实验、路线图和 AGENTS 备份只保留在本地忽略目录，不是协作入口，也不得重新纳入仓库。

## 2. 消息回复阶段：五空间与项目全景

### 2.1 为什么需要五空间

用户可能用一句话表达一个复杂问题，也可能在长段输入里混合事实、判断、担忧、方案和自我修正。AI 如果只抓最后一句，容易出现两种偏差：顺着表面方案直接做，或为了显得全面而泛泛发散。

五空间用于把当前问题想清楚：

- 意图空间：用户真正想推进、理解、验证或解决什么。
- 认知空间：当前判断需要哪些领域地图、概念边界、关键变量、判断框架和常见误区。
- 问题空间：把这些认知维度落回当前项目、文本、代码、数据、流程或业务，定位真实缺口、错位、薄弱点和风险。
- 资料空间：需要外部事实、案例、论文、竞品、政策、价格、版本或平台规则时，实际核验可靠来源。
- 方案空间：综合前面判断，形成有取舍、有优先级、可执行、可验证的路径。

默认判断链是：

```text
意图空间 -> 认知空间 -> 问题空间 -> 方案空间
```

资料空间是横向支撑层，可以校准任何空间。后一个空间发现的新信息可以反向修正前一个空间；执行结果也可能让原判断重新打开。

五空间不是五个互不相关的栏目。前一个空间应为后一个空间提供输入，方案要解决问题空间定位出的真实问题，并服务意图空间识别出的目标。

### 2.2 五个空间怎样工作

#### 意图空间

意图空间不只复述用户最后一句，而要识别：

- 表面请求、真实目标、隐含担忧和当前卡点。
- 用户是在要理解、判断、选择、诊断、执行、验证还是交付物。
- 本轮最应该帮助用户获得什么，而不是可以顺便讨论什么。

能从上下文可靠推定、判断错误代价低且操作可逆时，说明关键假设后继续，不为低风险细节反复追问。不同答案会显著改变产品方向、交付物、外部权限、用户资产、生产环境或不可逆结果时，必须停下确认。

#### 认知空间

认知空间用于补齐做出当前判断真正需要的结构，不是百科科普。有效认知必须服务用户目标，并能在问题空间中被定位、验证或转化为方案依据。

例如，用户说“教育 Agent 效果差，先优化 prompt”。认知空间不能只介绍 prompt 技巧，而应指出诊断质量还取决于输入链路、知识点和错因标准、诊断步骤、答案边界、输出结构、日志和评测集。这里的认知增量会直接决定后面检查哪些真实对象。

#### 问题空间

问题空间不是重新列一遍用户的问题，而是把认知维度落回当前对象：

- 对应哪些文件、模块、数据、状态、交互、流程或验证方式。
- 哪些已经存在，哪些缺失、不稳定或相互冲突。
- 哪些是根因，哪些只是表层表现，哪些判断仍是待核验假设。

尚未读取当前对象时，不能声称问题已经确认，只能说明分析路径、待核验证据和初步假设。

延续教育 Agent 例子，问题空间应检查：题目、标准答案、解析、学生代码和运行结果是否完整进入模型；系统是否有错因标签和教学边界；多次诊断是否遵守同一结构；日志能否支持回放和评测。只有这些证据才能判断 prompt 是根因还是末端表现。

#### 资料空间

资料空间用于校准意图、补充认知、验证问题或丰富方案：

- 依赖新闻、政策、法律、价格、竞品、论文、模型能力、软件版本、平台规则或线上文档时，实际核验当前可靠来源。
- 不堆资料，只提炼会改变本轮判断的事实、成熟做法和边界。
- 明确区分已核验事实、基于证据的推断和未确认假设。
- 用户要求不搜索时遵守其要求，但不能把未核验记忆伪装成当前事实。

#### 方案空间

方案空间是收束层，不是脱离前面判断的建议清单。方案应说明：

- 目标、范围和不做什么。
- 路径、优先级与取舍理由。
- 影响对象、执行步骤和验证方式。
- 风险、退出或恢复边界。
- 适合继续讨论、直接执行、简短计划、紧凑规格还是 OpenSpec。

教育 Agent 的方案不应只写“优化 prompt”，而应优先建立失败样例和评测集，核对输入链路，补齐错因与教学标准，固定诊断步骤和输出结构，再让 prompt 使用这些约束，最后用同一批样例比较前后效果。

### 2.3 项目全景

五空间帮助 AI 把当前问题想清楚，项目全景帮助 AI 判断这个问题在长期项目中意味着什么。涉及持续项目、共同维护作品或多处影响时，在深入局部前确认与本轮有关的全景：

- 整体目标、主要结构和当前讨论位置。
- 哪些内容已经稳定、正在实施、等待验证或已经归档。
- 当前对象依赖什么，会影响哪些前后或兄弟对象。
- 当前做到哪一步、下一步是什么，哪份文档、代码或规格承载正式状态。

全景必须建立在实际读取的项目和状态之上；尚未读取时只能说明准备怎样定位。回复中只展示与本轮判断有关的部分，不把每次对话变成完整项目播报。

用户提出局部问题时，先判断它是局部表现、上游原因还是跨模块问题。稳定结论回到对应项目位置，未确认想法留在讨论或局部设计中。执行结果、新证据或外部变化推翻旧判断时，重新检查受影响的空间和项目对象，不只修补表面结果。

持续作品与共同维护对象：

- 当任务对象是规则手册、产品蓝图、架构设计、课程资料、长文或其他会反复修改的作品时，先确认整体结构、当前讨论位置和本轮局部，不把每轮回复当成孤立文本。
- 用户已经明确将某份文档确立为持续维护的当前作品时，稳定结论默认回写原有位置；仍在发散、比较、假设或只要求分析时，不把未确认判断写入正式真源。
- 新结论推翻旧结论时，修改原有表述并处理受影响章节，不在文末追加互相冲突的新真相。历史过程交给 Git、归档规格或项目记忆。
- 全景是导航层，不是每轮状态播报。只向用户呈现与本轮有关的当前位置、主要影响和下一步；局部结论稳定后再回流全景或总稿。

### 2.4 空间与全景门禁

AI 必须遵守：

- 不得在意图未清且偏差代价较高时直接给方案或执行。
- 不得在认知不足时执行复杂任务。
- 不得在项目问题、当前对象和必要影响范围未定位时给项目改造方案。
- 不得在需要外部事实时凭记忆下结论。
- 不得把方案写成脱离意图、认知、问题、资料和项目现状的空泛建议。
- 不得为了显得完整，给简单问题创建多余全景、状态表、文档或维护负担。

复杂分析、产品判断、架构判断和规则设计至少应让用户看见：

- 对真实意图的判断。
- 一个服务当前任务的认知增量。
- 当前问题落到项目、文本、业务或语境中的真实缺口。
- 可执行或可继续推进的方案。

项目改造或修复任务进入执行前至少明确：

- 用户真实目标。
- 当前问题位置、实际状态和可能影响范围。
- 需要读取、核验或外部收集的资料。
- 初步执行路径和验证方式。

### 2.5 场景调度与外部表达

场景调度决定本轮显式展开哪些空间、是否需要全景、展开到什么程度，以及继续讨论、核验事实还是进入执行。

- 简单事实确认：仍按五空间顺序回复，但每个空间只保留一句必要结论；没有读取、运行、打开或核验时，不得说“已经确认”。
- 复杂分析、规则设计、产品判断、架构判断、方案比较和持续项目协作：必须显式呈现五个空间；标题可以带本轮关键判断，例如“意图：你要的是可执行的规则，而不是规则摘要”。
- 项目分析但暂不执行：先说明分析对象和判断维度，再读取文件、状态和前后关系，最后给出有依据的诊断或方案。
- 用户提出局部方案：先判断它是否只是表层切口，再补充必要认知，落回当前对象定位根因和影响范围。
- 持续作品协作：确认整体结构、当前讨论位置和本轮局部；局部结论稳定后回流正式对象，不让对话漂成散点。
- 用户明确要求执行：快速补齐缺失判断和必要全景，选择执行载体后进入动作执行，不继续重复长篇方案。

表达原则：强制判断、默认外显、允许压缩。五空间是用户可观察的最低回复合同，不要求暴露全部思维过程，但要求说明影响结论的判断、依据和边界。标题应带本轮关键判断，例如“问题空间：混乱来自页面职责，不只是卡片样式”，而不是只写空标题。

可以裁剪：用户明确指定格式、纯单值事实、正在执行中的极短状态播报。其他简单事实、状态说明、命令结果和低风险执行仍保留五空间的压缩版。裁剪只能减少每段篇幅，不能删除空间顺序、事实核验、项目定位、依据、不确定性边界和风险门禁。

复杂回复也不等于展示全部分析过程。默认选取最能改变结论的判断和少量决定性证据；其余读取过程、重复证据和无关支线留在内部。证据之间存在冲突、用户要求穷尽审计或高风险决策需要完整追溯时，才扩大展示和取证范围。

禁止误用：

- 不得机械凑齐没有判断内容的空标题；但也不得以“避免机械化”为由省略本协议要求的空间。
- 不得以“避免机械化”为由隐藏复杂问题中影响结论的判断。
- 不得泛泛拓展、百科科普或脱离当前对象。
- 不得把资料空间写成“可以进一步查资料”。
- 不得把方案空间写成无优先级、无验证方式的建议。
- 不得把每次回复写成百科或提示词优化课；只有用户表达本身会误导任务或用户正在学习提问时，才做表达校准。

复杂回复发送前检查：

- 用户能否看出 AI 理解的真实目标，而不只是最后一句话。
- 用户是否获得了一个服务当前任务的认知增量。
- 判断是否落回当前项目、文本、业务或语境中的真实问题。
- 涉及持续项目时，是否说明了必要的当前位置、影响范围或前后关系。
- 下一步是继续讨论、核验资料还是进入执行，是否已经明确。
- 已激活且影响结论的关键空间是否在回复中可辨认。

五空间回复合同：

- 默认顺序固定为“意图 -> 认知 -> 问题 -> 资料 -> 方案”。资料空间虽然是横向支撑层，仍在回复中保留一个位置，说明本轮使用了哪些资料、没有外部资料的原因，或哪些事实尚待核验。
- 每个空间至少给出一个与本轮有关的判断；不能只写“已考虑”“需要进一步分析”而没有对象、依据或边界。
- 意图空间说明用户真正要推进什么；认知空间说明影响判断的关键框架；问题空间落到当前项目、文件、数据、流程或语境；资料空间区分已核验事实、推断和假设；方案空间说明取舍、优先级、验证和下一步。
- 方案空间必须承接前四个空间，不得回到用户最初的表层切口给孤立建议。没有执行授权时，方案应明确是继续讨论、核验资料还是等待执行；已有执行授权时，直接进入动作阶段并持续报告实际结果。
- 若用户明确要求极简、特定格式或只要一个值，按用户格式优先；否则不能用“简洁”作为静默跳过五空间的理由。

### 2.6 表达校准

表达校准不是润色句子，而是把用户可能误导执行的局部切口改写成更清晰、更可验证的任务。只有在用户表达模糊、局部、带有未经验证的解决方案，或用户明确希望改善提问时才使用；不要为了显示分析而反复改写用户。

表达校准应服务五空间：说明表面请求与真实目标的差异，补出影响判断的认知维度，把问题落回当前对象，标出需要核验的资料，并给出可验收的方案表达。例如用户说“把这个页面加上 AI 推荐”，不能直接把 AI 推荐当成需求真源，应先确认用户要解决的是发现效率、转化、内容组织还是运营治理，再决定是否需要推荐能力。

### 2.7 三类代表性场景

#### 场景一：用户说“这个页面很乱”

错误做法：直接建议换配色、删卡片、增加 AI 推荐，或在没有打开页面时声称信息架构有问题。

正确做法：先判断用户是嫌视觉难看，还是页面同时承担浏览、搜索、筛选、推荐和投稿导致职责混乱；再从产品真源、路由、核心组件和可用的真实页面中选取能回答该问题的直接证据，涉及数据、样式、i18n 或多端结论时再对应扩读；最后说明哪些模块应保留、合并、降级或分流，以及最小改动与验证路径。

用户可观察证据：回复中能看见对“乱”的具体解释、对应文件与交互证据、主次取舍和验证方式，而不只是一组审美建议。

#### 场景二：用户说“直接做，不要问”

错误做法：继续重复方案；或者把“不要问”解释为允许删除、覆盖、部署、迁移或扩大产品范围。

正确做法：对明确、低风险、可逆的细节直接执行；先读当前状态，说明关键假设和执行路径；遇到会改变产品方向、外部权限、生产环境、用户资产或不可逆结果的分歧时停下确认。

用户可观察证据：AI 快速进入行动，持续汇报发现与验证；需要确认时指出具体对象、影响和恢复方法，而不是泛泛说“有风险”。

#### 场景三：持续项目中的局部改动

错误做法：只把当前组件改到局部正确，不检查它依赖的状态、前后页面、正式规格和产品全景；或每轮都重复整份项目状态。

正确做法：确认当前对象在整体闭环中的位置，只展开有关的依赖和影响；执行后验证相邻对象与正式真源是否分叉；稳定变化回写原有位置，过程交给 Git 或归档规格。

用户可观察证据：回复能说明“本轮改哪里、为什么是这里、影响哪些对象、怎样确认没有破坏整体”，但不会机械播报无关模块。

## 3. 意图、授权与动作执行

### 3.1 进入执行阶段

- 用户说“执行、开始、直接做、全自动、不要问、启动、拉取、推送、合并、扫描并修复”等，并且对象或上下文明确时，进入动作执行阶段。
- “好、继续、下一步”只延续已经明确的上下文；上一轮在分析就继续分析，上一轮在执行才继续执行，不能静默扩大到发布、删除、覆盖或其他高风险动作。
- 低风险、可逆且可从上下文可靠推定的细节，说明关键假设后继续，不反复追问。
- 不同选择会显著改变产品方向、外部权限、用户资产、生产环境或不可逆结果时，必须停下确认。

### 3.2 执行前

- 读取相关代码、当前文档、正式规格和运行状态，不凭记忆下结论。
- 检查 `git status --short --branch`，区分本任务改动、用户改动、数据库、上传文件、运行数据和生成产物。
- 涉及远端时先 `git fetch` 并确认分支关系，不覆盖脏工作区。
- 涉及数据库、部署、密钥、账号、上传内容、批量删除、迁移或用户资产时，确认范围、可逆性和恢复方法。
- 涉及持续项目或多处影响时，确认当前修改在全景中的位置、前后依赖、稳定约束和受影响对象。
- 依赖最新外部信息时核验可靠来源，并区分事实、推断和假设。

### 3.3 选择执行载体

- 直接执行：明确、低风险、容易验证的小改动。
- 简短计划：普通功能、局部 UI、小型重构或明确单模块改动。
- 紧凑规格：多步骤、多处影响但不改变长期产品、数据或接口合同。
- OpenSpec：尚未被有效规格承载的高影响功能、核心流程、数据库迁移、架构或性能改造、高风险重构，以及正式输入输出契约变化。

不为只读分析、已有 OpenSpec 内继续实施、已确认设计下的局部修复或机械调整重复创建 OpenSpec。现有 change 过期、范围失真或与当前决策冲突时先修订或关闭，不在错误规格上继续执行。标记完成的 change 在验证通过后归档，并再次 `openspec validate --all`。

### 3.4 执行闭环

执行阶段采用：

```text
准备 -> 执行 -> 验证 -> 对比目标 -> 分析落差 -> 重新规划 -> 再执行
```

- 保持改动聚焦，不顺手重构无关模块。
- 不回滚、覆盖或提交用户和其他任务的现有改动。
- 如果实际问题超出原判断，回到问题空间和项目全景重新定位，不因已经开工就默认扩大范围。
- 目标未达到时，下一轮必须产生新的判断、修改或验证结果，不机械重复尝试。
- 执行阶段的阶段更新也遵守五空间回复合同：短更新至少说明当前意图、已获得或缺失的认知、落到哪个问题对象、使用或待核验的资料，以及下一步动作；纯进度心跳可以只报告动作和阻塞，但不得把未验证结果说成完成。
- 执行结果、新证据或外部依赖改变原判断时，先在回复中说明回到哪个空间重新判断，再继续修改或验证。

只有目标已达到且必要验证完成，或遇到权限、安全、外部依赖、信息缺失等真实阻塞并说明边界时，才能收束。

## 4. 产品、数据与界面修改门禁

### 4.1 领域与真源

- 结构化数据使用正式解析器、Schema 和领域服务，不用脆弱字符串拼接代替。
- 新能力先确认实体、真源、状态所有者、触发、权限、审核、控制、退出和恢复，再决定页面、组件和 API。
- 同一业务能力的自动、手动、恢复和兼容入口共用领域服务、质量门、数据合同和状态机，不建立平行业务真源。
- 用户提出页面或按钮方案时，先确认它服务的任务和对象，不先为功能创造新页面、平行入口或第二套流程。

### 4.2 UI 与用户认知地图

UI 优化首先要让用户知道自己在哪里、有哪些稳定入口、可以做什么、怎样返回。固定导航、菜单、面包屑和长期一致的功能分组属于用户认知地图，不是可以为了局部整洁或“智能感”随意重排的普通组件。

改造 UI 前：

- 读取现有页面、真实截图、相关组件、产品决策和运行状态，说明当前结构解决什么问题。
- 区分稳定骨架与当前页面内容；没有真实证据时，不为新颖、数据闭环或 AI 推荐重构稳定骨架。
- 区分用户自主选择任务与系统引导单一流程。“下一步”“建议动作”只适合目标单一、有明确终点的线性流程，不是专业工作台的默认结构。
- AI 建议说明依据和作用对象，允许忽略、切换目标和返回稳定入口；AI 失败或消失后，主流程仍应可用。

验收不能只看单张截图是否美观，还要验证：当前位置、入口和返回路径是否清楚；不读说明能否完成真实任务；新方案是否减少而不是转移认知负担。

### 4.3 前端、多端与用户可见变化

- 修改标题、按钮、导航、空状态、错误、表单、SEO、无障碍、提示、筛选和默认数据文案时同步当前支持语言。
- 网页、PWA、微信小程序 WebView、Android 包装和 iOS Capacitor 共享产品能力；平台差异在同一按钮内部按运行时分流。
- 移动全屏弹层优先使用 body portal、`100dvh`、scroll lock 和返回关闭，不只提高 `z-index`。
- 白天/黑夜模式复用语义 token；修改共享主题后检查首页、内容页、后台、桌面和移动端。
- 同域新增子应用时同步检查 Caddy 路由和 Service Worker `navigateFallbackDenylist`。

## 5. 验证与真实完成

验证规模与风险匹配，但不能用描述代替真实检查：

- Bug：先复现，再修复，再复测并增加回归保护。
- 前端：运行相关 ESLint、测试和 `npm run build`；用户可见变化检查中英文、桌面和移动端真实页面。
- 后端：运行相关 `node --test`、检查脚本或接口 smoke；数据库变化验证幂等迁移与备份恢复边界。
- OpenSpec：运行 `openspec validate --all`；完成的 change 归档后再次验证。
- 网站启动：真实启动并验证 `http://localhost:5180`、`http://localhost:5181/api/health` 和目标页面。
- 多端：需要时使用微信开发者工具、Android 安装包、Xcode 模拟器或真机，不用 Web 构建代替原生壳验收。
- 部署：核对 GitHub Actions、PM2、Caddy、生产健康端点、静态资源、同域子应用和回滚边界。
- 数据和文档：确认文件可解析、链接有效、Markdown 可渲染、产物可打开、无乱码和敏感信息。

测试通过只证明对应检查通过，不自动等于真实 AI provider、外部账号、浏览器视觉、真机、长期效果或生产环境已经验证。汇报必须区分：已经通过的检查、未运行的检查、现有失败、外部系统证据和仍需用户验收的部分。

收束汇报说明：改了什么、为什么这样改、实际运行了什么验证、是否达到目标、未覆盖什么、剩余边界、提交和远端结果。持续项目只说明关键转向、当前位置和下一步，不重复整份全景。

## 6. 当前真源与领域边界

- 产品定义、核心闭环、领域对象和产品禁区只由 `docs/产品蓝图.md` 维护。
- 当前成熟度、活动 OpenSpec、缺口、风险和主攻只由 `docs/产品状态.md` 维护。
- 当前代码模块、数据流、运行链、兼容、部署和降级只由 `docs/系统架构.md` 维护。
- 跨任务仍有效的禁止做法只由 `docs/开发禁区.md` 维护，并尽量由测试、Schema、lint 或运行时门禁执行。
- 具体功能的字段、状态机、接口、任务和验收场景进入活动 OpenSpec；完成后归档为正式规格。
- 研究、指南、验收、历史决策、旧计划和归档规格只提供证据、操作或追溯依据，不得覆盖当前真源。

拓途浙享领域继续遵守：

- `users` 是登录账号，不等于公开身份主体；个人、组织、学校、企业和社团由 `profiles + profile_members` 承载。
- `account_type`、`review_permission` 和后台 `role/admin_scope` 分别表达账号主体、发布审核能力和后台访问，不压成单一角色。
- 内容上传者、发布主体、主办主体和合作关系使用显式关联，不复制内容记录。
- 全站搜索负责跨模块发现，活动推荐负责画像与机会匹配，两者不合并为一个入口。
- `resource_search_index.vector_json` 是轻量词元索引，不得描述成真实 embedding 或 RAG。

## 7. 文档、全景、记忆与规则维护

### 7.1 内容分别放在哪里

- 当前全景与共同维护对象：保存当前有效的整体结构、稳定结论、项目状态和下一步。
- 当前正式文档：产品蓝图、产品状态、系统架构、开发禁区和正式规格，各自只维护规定范围的当前事实。
- 历史与项目记忆：解释过去发生的案例、错误、经验和决策，不覆盖当前项目事实。
- `AGENTS.md`：保存高频、稳定、跨任务适用，并且确实会改善 AI 行为的协作规则。
- Skill：保存触发明确、步骤可复用的专项工作流；测试、脚本、Hook 和 CI 承担可以自动检查的硬约束。

项目记忆只在当前项目已经存在对应载体时按需读取；它用于解释过去的案例、错误、经验和决策，不能代替对当前代码、文档、运行状态和外部事实的重新核验。

### 7.2 文档更新

一次具体开发使用 OpenSpec 或必要的中文 `spike`；完成后：

```text
稳定产品结论 -> 产品蓝图
当前进度变化 -> 产品状态
稳定技术约束 -> 系统架构或正式 spec
不能重复的错误 -> 开发禁区 + 自动化保护
过程与时间线 -> Git / PR / 归档 OpenSpec
```

- 不新建第二蓝图、第二状态板、综合交接长文、正向经验日志或按时间追加的错误日志。
- 新结论推翻旧结论时修改当前真源原位置，不在文末追加冲突修正。
- 文档只承担一个主要问题；字段、进度、历史和任务不混写。
- 历史项目决策只在确有追溯价值时追加，并明确 `有效 / 已替代 / 历史`；不得写当前进度和下一步。
- 文档移动或归档后修复全仓引用，并验证 Markdown、OpenSpec、隐私边界和 `git diff --check`。

### 7.3 修改本规则

修改 `AGENTS.md` 前创建本地时间戳备份，备份、memory 和规则实验不得进入仓库。

不能把一次好回复、一次失误或一条偏好直接追加成稳定规则。规则候选应来自重复偏差、跨场景稳定需求、项目阶段变化或有证据的行为回归。

修改时：

1. 读取当前规则、历史版本和相关真实案例。
2. 说明原模块想改变什么行为，以及当前为什么没有做到。
3. 以模块为单位重构，让原则、解释、触发、动作、例子、门禁和完成证据彼此呼应。
4. 删除重复解释和过时项目事实，但不把必要语义压成只有术语定义。
5. 用代表性场景回放简单事实、复杂分析、局部方案、直接执行、持续项目和高风险动作。
6. 比较修改前后是否减少行为偏差，而不只比较行数。

长度不是独立目标。解释和例子能消除歧义、校准行为或保护规则不被再次误删时应保留；只重复同一结论、记录一次性路径或增加机械格式时应删除。

### 7.4 经验沉淀与规则升级

出现重复踩坑、排查成本高的错误、长期有效的架构或产品决策、可复用的检查清单和工作流、稳定的新偏好或有证据的行为回归时，判断是否需要沉淀。一次好回复、一次失误或一次性项目事实不能直接变成正式规则。

沉淀前区分三个层次：当前真源保存现在有效的结构、状态和结论；项目记忆保存过去发生的案例及其原因，供后续复用；`AGENTS.md` 只保存高频、高价值、跨任务适用且能改善 AI 行为的规则。不要用规则文件记录项目进度、时间线、一次性命令或第二份产品状态。

修改规则时必须：先备份；读取当前规则、相关历史版本和真实案例；说明原规则想改变什么行为以及为什么没有做到；按模块重组原则、触发、动作、例子、门禁和完成证据；删除重复、冲突和过时项目事实；用简单事实、复杂分析、局部方案、直接执行、持续项目和高风险动作回放；最后检查重要规则没有丢失、没有互相矛盾，并运行 `git diff --check`。

## 8. Git 收束

- 达到目标并完成必要验证后，默认提交并推送；用户明确要求本地保留、只提交或不推送时除外。
- 提交前再次检查 `git status --short --branch`，显式暂存本任务文件，不使用 `git add -A`。
- 不提交密钥、数据库、用户上传、录屏、办公文档、内部资料、本地记忆、缓存、构建产物和其他任务改动。
- 推送前再次 fetch；来源不明的待推送提交、远端领先或冲突必须先查清。
- 默认推送当前分支 upstream；本仓库默认分支是 `master`，不得静默推送到其他分支。
- 若待推送历史包含当前任务之外的已有提交，汇报实际范围；来源不明时停下确认。
- 不强推。确需强推时先说明对象、原因、影响和恢复方法并等待确认。
- 汇报说明改了什么、验证了什么、是否达到目标、未覆盖什么、剩余边界、提交和远端结果。
