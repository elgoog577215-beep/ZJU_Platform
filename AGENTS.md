# 拓途浙享 AI 协作规则

> 本文件是 AI Agent 的协作操作手册，也为人类协作者提供可读的规则总览。
>
> 核心循环：**回复 -> 用户确认 -> 执行与验收 -> 迭代回写 -> 下一轮回复**。
>
> 当前事实优先级：代码、运行结果、数据库 schema、正式文档和 OpenSpec 高于记忆、对话和历史材料。

## 目录

- [一、总览](#一总览)
- [二、回复：五步回复法](#二回复五步回复法)
- [三、执行与验收](#三执行与验收)
- [四、迭代：文档系统与规则升级](#四迭代文档系统与规则升级)
- [五、本项目真源与稳定边界](#五本项目真源与稳定边界)

## 一、总览

### 1.1 三个阶段

| 阶段 | 进入条件                                                           | 主要动作                                         | 结束条件                           |
| ---- | ------------------------------------------------------------------ | ------------------------------------------------ | ---------------------------------- |
| 回复 | 用户提出问题、想法或方案，尚未确认执行                             | 定位项目、解读意图、提升认知、判断问题、提出方案 | 方案清楚，等待用户确认             |
| 执行 | 用户明确说“执行、开始、直接做、继续执行、修复、推送”等，且对象清楚 | 读取现状、按方案修改、按风险验收、处理落差       | 目标达到并完成必要验收，或明确阻塞 |
| 迭代 | 执行结果产生稳定结论，或用户提出新的长期要求                       | 更新正确文档、沉淀经验、必要时升级 `AGENTS.md`   | 真源已回写，下一轮可从稳定状态继续 |

用户说“好、继续、下一步”时，只延续上一轮已经明确的阶段，不静默扩大到发布、删除、覆盖或其他高风险动作。

### 1.2 通用原则

- 默认使用中文。路径、命令、包名、模型 ID、接口字段、代码标识和行业术语可以保留英文。
- 用户可见前端文案必须通过现有 i18n 同时维护中文和英文。
- 用户输入通常只是局部切口。AI 要理解真实目标，但不能借“理解意图”擅自扩大范围。
- 需要外部事实时实际核验；必须区分已核验事实、证据推断和未确认假设。
- 读取当前代码、文档、规格、运行状态和工作区后再判断，不凭记忆修改。
- 保持改动聚焦，不回滚或覆盖用户及其他任务的改动，不创建平行业务真源。

### 1.3 风险门槛

低风险、可逆且容易验证的细节，说明关键假设后直接推进。涉及产品方向、生产环境、外部权限、用户资产、数据库、密钥、账号、批量删除、强制覆盖或其他不可逆结果时，先说明影响、范围和恢复方式，必要时停下确认。

## 二、回复：五步回复法

回复发生在执行之前。复杂分析、产品判断、架构判断、规则设计、方案比较和持续项目协作，必须让用户看见以下五个步骤；简单问题可以压缩，但不能省略影响结论的必要判断。

### 2.1 输出格式

五步使用最高级标题和中文编号、顿号：

```markdown
# 一、项目全景

# 二、意图

# 三、认知

# 四、问题

# 五、方案
```

标题可以补充本轮判断，例如 `# 一、项目全景：当前位于模块 5 的 5.3 小节`。不暴露完整思维过程，只呈现影响结论的判断、依据、不确定性、修改方向和预期结果。

### 2.2 五步分别回答什么

#### 一、项目全景

说明项目总结构、当前模块、当前小节、本轮对象、影响范围和下一位置。涉及持续项目、产品设计、架构、规则手册、长文或多处影响时，回复开头必须定位全景。

项目蓝图回答项目是什么、服务谁、核心闭环和长期模块；产品状态/项目全景回答现在走到哪个阶段、模块和小节，哪些已完成、在建或待验证，以及下一步是什么。首次进入项目、模块切换或阶段变化时可以展示完整目录；连续讨论只显示当前路径；一次性简单问题不强行播报全景。

#### 二、意图

说明用户表面说了什么，真正想推进、理解、选择、验证或解决什么，以及本轮最应该得到什么。不能只复述最后一句，也不能把用户提出的局部方案直接当成最终需求。

#### 三、认知

补充服务本轮的更高层判断框架、概念边界、关键变量、常见误区和可迁移标准。认知用于提升判断力，不做脱离当前任务的百科介绍。

#### 四、问题

把前面判断落回当前项目、文本、代码、数据、流程或业务，说明现在到底是什么问题、证据是什么、影响哪些对象，以及哪些仍是假设。未读取当前对象时，不能声称问题已经确认。

#### 五、方案

说明准备怎么修改或推进、修改哪些对象、为什么这样取舍、修改后得到什么结果，以及如何确认结果。方案必须承接项目全景、意图、认知和问题；回复阶段结束时方案清楚并等待用户确认，不把讨论结果说成已执行。

### 2.3 资料按需嵌入

资料不是固定的第六步。只有需要查最新新闻、数据、线上规则、竞品、论文、版本、平台信息或其他外部事实时才启动资料层，并嵌入相关步骤。

查过资料时说明来源、查到的关键事实、事实与推断的区别，以及资料怎样改变本轮意图、认知、问题或方案。没有查外部资料时，不单独列资料。

### 2.4 回复前检查

- 是否先定位了项目总结构和当前模块/小节。
- 用户能否看出真实意图，而不只是最后一句话。
- 是否提供了服务本轮的认知增量。
- 问题是否描述当前真实状态，而不是重复表面说法。
- 方案是否说明修改动作、预期结果和确认方式。
- 如使用外部资料，来源、事实、推断和假设是否分清。

## 三、执行与验收

### 3.1 执行前

进入执行后，先补齐真正缺失的目标、范围、当前问题位置、影响对象、必要资料、执行路径和最低验收等级，不重复已经确认的长篇方案。

涉及新能力时，先确认实体、正式真源、状态所有者、权限、审核、退出和恢复，再决定页面、组件和 API。结构化数据使用正式解析器、Schema 和领域服务。

涉及 UI 时，先确认真实页面、稳定导航、用户任务和运行状态；不因“AI 推荐”“下一步”或视觉整洁而重排稳定导航和用户认知地图。涉及多端时，Web、PWA、微信、Android 和 iOS 复用同一产品能力，平台差异在同一入口内部按运行时分流。

### 3.2 验收分级

验收以用户目标、风险、影响范围和返工成本为尺度。GPT-5.6 不得把所有任务自动升级为完整工程验收。

| 等级               | 适用范围                                                                          | 最低动作                                                                                     |
| ------------------ | --------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| 一级：轻量验收     | 极小、低风险、结果直接可见且容易撤回的规则措辞、文案、单点整理                    | 检查文件状态、差异内容和基本语法，不额外启动全量测试、浏览器或多端检查                       |
| 二级：常规验收     | 大多数普通功能、局部 UI、单模块修复和文档变更                                     | 运行与改动直接相关的 lint、测试、构建、smoke、解析或页面检查，检查实际涉及的语言、平台和交互 |
| 三级：完整工程验收 | 多模块、高风险、核心流程、数据库迁移、架构/性能、跨端、复杂 AI 流程或正式合同变化 | 按需要运行完整测试、浏览器流程、多端验证、迁移回放、性能检查、OpenSpec 验证或真实环境 smoke  |

可在确有稳定需求后增加专项验收模式，例如 UI 视觉、移动端、AI provider、数据迁移或生产环境验收；不因一次任务临时扩张全局规则。

汇报时区分：已通过、未运行、已有失败、外部系统证据和仍需用户验收的部分。不能运行的检查要说明原因，不能把推断说成完成。

### 3.3 执行闭环与收束

```text
准备 -> 修改 -> 按等级验收 -> 对比目标 -> 处理落差
```

目标未达到时，下一轮必须产生新的判断、修改或验证，不机械重复。执行结果改变原判断时，回到项目全景、当前问题或方案重新定位。

达到目标且完成对应等级的必要验收，或遇到明确的权限、安全、外部依赖和信息阻塞时收束。汇报说明改了什么、为什么、验证了什么、是否达到目标、剩余边界和下一步。

### 3.4 Git 收束

- 检查工作区和远端关系，只暂存本任务文件，不使用 `git add -A`。
- 默认提交并推送当前分支 upstream，除非用户明确要求只提交或保留本地。
- 不强推，不把来源不明的提交带入本次推送。
- 提交前运行 `git diff --cached --check`，汇报提交和远端结果。

## 四、迭代：文档系统与规则升级

迭代的目标不是记录所有过程，而是让稳定结论回到唯一正确的载体，使下一轮 AI 能从稳定状态继续。

### 4.1 文档系统：读取、分类、回写、检查

#### 第一步：读取当前真源

按任务范围读取必要文档，不机械读取全部历史：`README.md` 负责入口；产品蓝图负责产品定义；产品状态负责当前阶段和模块位置；系统架构负责代码、数据和运行链；开发禁区负责跨任务禁区；OpenSpec/正式 spec 负责具体功能合同。历史材料、归档规格和备份只在追溯原因或复现现场时读取，不能覆盖当前真源。

#### 第二步：判断信息类型

```text
稳定产品语义、核心闭环、长期模块和产品边界 -> docs/产品蓝图.md
当前阶段、模块/小节位置、成熟度、缺口和下一步 -> docs/产品状态.md
代码结构、数据流、运行链、部署和稳定技术约束 -> docs/系统架构.md
具体字段、接口、任务、状态机和验收合同 -> OpenSpec / 正式 spec
错误、经验、决策、灵感和历史原因 -> 本地候选记忆 / docs/归档/
稳定且跨任务的 AI 行为要求 -> AGENTS.md
```

#### 第三步：写回唯一载体

- 产品蓝图不按日期追加进度；只有产品定义、长期模块或边界改变时才更新。
- 产品状态只写当前动态位置、完成情况、待验证项和下一步，不复制产品定义。
- 系统架构只写代码、数据、运行链、部署和稳定技术约束。
- OpenSpec/正式 spec 写具体功能合同，完成后归档。
- 本地候选记忆只用于暂存和复用，不进入公开仓库；稳定历史原因进入 `docs/归档/`，两者都不覆盖当前真源。
- `AGENTS.md` 只写稳定、跨任务且确实能改善 AI 行为的协作规则。

#### 第四步：检查重复与冲突

- 不创建第二蓝图、第二状态板、第二事实源或按日期追加的综合日志。
- 新结论推翻旧结论时，修改当前真源原位置，不在文末追加冲突版本。
- 一次好回复、一次失误或一次性事实不能直接升级为正式规则；必须经过重复验证、跨任务适用、有行为回归证据，或得到用户明确要求。
- 文档更新后检查引用、链接、Markdown、隐私边界和当前项目状态。

### 4.2 `AGENTS.md` 自我升级

当用户提出新的稳定要求，或执行中出现可复用的行为缺口时，先判断它应该进入产品蓝图、产品状态、系统架构、开发禁区、OpenSpec、项目记忆、专项 Skill，还是 `AGENTS.md`。

如果确实属于 `AGENTS.md`：

1. 备份当前文件，备份不进入仓库。
2. 阅读当前规则、相关历史版本和真实案例。
3. 说明原规则想改变什么行为、当前哪里没有承载住。
4. 按“原则 -> 触发 -> 必须动作 -> 完成证据”重组。
5. 删除重复、冲突、过时项目事实和不必要的维护负担。
6. 先向用户说明准备怎样改；用户确认后执行，或在用户已明确授权持续维护时直接执行。
7. 用项目全景、复杂回复、局部方案、直接执行、持续项目和高风险动作回放新规则。
8. 检查 Markdown、引用、隐私边界、`git diff --check` 和实际行为，再汇报结果。

## 五、本项目真源与稳定边界

### 5.1 真源入口

- `README.md`：人类入口、安装、启动、测试和协作入口。
- `docs/产品蓝图.md`：产品定义、核心闭环、长期模块和领域边界。
- `docs/产品状态.md`：当前阶段、模块位置、成熟度、缺口、风险和下一步。
- `docs/系统架构.md`：代码结构、数据流、运行链、部署和技术边界。
- `docs/开发禁区.md`：跨任务仍有效的产品与工程禁区。
- `openspec/changes/<change>/` 与正式 spec：具体功能的字段、状态、接口、任务和验收。
- `docs/指南/`：可重复执行的发布、采集、打包和验收流程。
- `docs/归档/`：历史决策、旧规格和追溯材料，不覆盖当前事实。

### 5.2 稳定领域边界

- `users` 是登录账号，不等于公开身份主体；公开主体由 `profiles + profile_members` 承载。
- `account_type`、`review_permission` 和后台 `role/admin_scope` 分别表达主体、审核能力和后台访问。
- 上传者、发布主体、主办主体和合作关系使用显式关联，不复制内容记录。
- 全站搜索负责跨模块发现，活动推荐负责画像与机会匹配，两者不合并。
- `resource_search_index.vector_json` 是轻量词元索引，不描述成真实 embedding 或 RAG。
- AI 输出默认是建议、筛选条件或候选；正式内容、主体、权限和迁移仍经过领域服务、审核和权限检查。
- 数据库、WAL、备份、上传目录、密钥、用户数据、商业文书、会话记录和本地 memory 不进入公开 Git。
