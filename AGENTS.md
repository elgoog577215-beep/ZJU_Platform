# 拓浙 AI 生态数字平台项目规则

```text
项目 AGENTS.md
├── 一、文件体系
│   ├── 项目文档
│   └── 操作手册
├── 二、更新机制
│   ├── 文档更新
│   ├── 事实记录
│   └── 规则沉淀
└── 三、项目规则
    ├── 项目边界
    ├── 执行规则
    ├── 验收规则
    ├── 前端与多端规则
    ├── 正式材料
    └── 未分类规则（始终放在最后）
```

## 一、文件体系

### 1.1 项目文档

项目文档记录项目是什么、现在做到哪里以及技术怎样运行：

1. `README.md`：项目简介、安装、启动、测试和基本使用入口。

2. `docs/产品蓝图.md`：产品定位、各模块功能设计和模块之间的逻辑关系。

3. `docs/产品状态.md`：产品各模块的当前开发进度。

4. `docs/技术架构.md`：代码结构、数据库等技术架构。

5. `AGENTS.md`：本项目的长期协作规则与案例。

### 1.2 操作手册

操作手册记录已经稳定、可以重复执行的具体工作流。现有手册位于 `docs/操作手册/`，包括 CLI 投稿、应用打包、内容采集和移动端验收。

每份手册写清前置条件、操作步骤、关键命令、验收方法和常见问题。项目变化写入项目文档，长期规则写入 `AGENTS.md`；可以自动化的步骤优先写成脚本、CI 或命令。

## 二、更新机制

不同内容写入对应位置：

```text
产品设计 → docs/产品蓝图.md
模块进度 → docs/产品状态.md
技术架构 → docs/技术架构.md
具体功能开发 → OpenSpec
执行结果和用户反馈 → docs/事实.md
长期协作规则 → AGENTS.md
```

1. 记录事实：重要执行结果和用户明确反馈写入 `docs/事实.md`，尤其记录用户认为成功或失败的结果。每条事实包括时间、背景、动作、结果、反馈和证据。

2. 提炼规则：用户明确提出长期要求，或者多个事实证明同一种方法有效时，将其写入本项目 `AGENTS.md`。每条规则尽量写清触发条件、必须动作和完成证据，只说明行动与必要边界，不记录过程故事。

3. 归类规则：新规则写入最合适的章节；暂时无法归类时放入“未分类规则”。同类规则积累后，再整理成新的分类。

4. 保持轻量：新规则优先合并或替换旧规则。可以通过测试、CI 或正式合同保证的内容，不重复写入 `AGENTS.md`。修改文件体系、更新机制、核心项目边界或通用规则前，需要用户确认。

## 三、项目规则

### 3.1 项目边界

1. 产品定位：平台是拓浙 AI 生态的数字底座，连接学生、组织、学院、企业、AI 需求和产业机会，不做单纯的活动聚合站或新闻站。

2. 品牌名称：公开品牌使用“拓浙 AI 生态”；“拓途浙享”只表示数字平台，不代替线下生态、商业交付或学校职责。

3. 实体边界：拓浙 AI 生态是校园、教育与实践母系统；拓途浙享是数字底座；智能体协会是校内组织；奇鹰科技承接对外商业合同、交付、收款、合规和 IP。四者可以协作，但不能互相代称或归并。

4. 业务闭环：活动、AI 社区、项目广场、浙客松、profile、搜索推荐和后台属于同一套生态系统，不能作为孤立栏目分别建设。数字底座应让人找到机会、组织找到人、内容找到归属、成果得到沉淀。

5. 内容与项目：网页、文档、CLI 和微信采集共用内容审核与归属链。项目、活动、主体、成果和合作关系使用独立实体，不能用帖子、收藏、海报或媒体文件代替。

6. 赛事与项目：`competition_works` 保存参赛快照，`project_cards` 保存长期项目，两者只通过明确的 `project_id` 关联。未确认归属的作品继续作为快照保存，不能根据标题、姓名或 Git 链接猜测所有权。

7. 身份与权限：`users` 负责登录，`profiles + profile_members` 负责个人和组织主体。`account_type`、`review_permission` 和 `role/admin_scope` 分别管理主体类型、审核能力和后台权限。

8. 搜索与推荐：全站搜索负责跨模块查询，活动推荐负责根据用户画像寻找机会，两者不合并。`resource_search_index.vector_json` 是轻量词元索引，不是 embedding 或 RAG。

9. AI 边界：AI 只生成建议、条件或候选，不能直接写入正式内容、归属、权限和迁移结果。

10. 数据：Schema 由核心建表和正式 migration 管理，不在 controller 或请求过程中临时改表。

11. 多端：Web/PWA 是产品和代码主真源，微信、Android 和 iOS 复用同一套产品能力和用户入口。

12. 部署：推送 `master` 后由 GitHub Actions 部署，生产入口使用 Caddy，不恢复已经退役的 Nginx 流程。

### 3.2 执行规则

1. 执行方式：文案、文档、局部样式和单点错误采用快速执行；核心业务、跨模块改造、数据库迁移、权限、API 和多端交付使用 OpenSpec。

2. OpenSpec：已有 change 覆盖当前任务时继续使用，不新建平行方案。历史 OpenSpec 只用于追溯。

3. 新能力：先确定业务实体、数据真源、状态归属、权限、审核、退出和恢复方式，再设计页面和 API。

4. 新入口：复用已有主体、内容、项目、审核和权限链，不复制业务事实。

5. 数据库：变化进入核心 schema 或正式 migration，并保证幂等、备份、回退和失败可见。

6. 写操作：上传、报名、收藏、审核和迁移不默认重试；确认幂等后才能重试。

7. 多语言：用户可见文案通过现有 i18n 同时维护中文和英文。

8. 多端入口：保持一个用户入口，再根据 Web、PWA、微信、Android 或 iOS 选择对应实现。

9. Git：只暂存本任务文件，提交前检查范围和 `git diff --cached --check`。任务完成后安全推送 `origin/master`，不覆盖远端提交。

10. 登录凭据：token 默认使用 `sessionStorage`，用户选择持久登录时才使用 `localStorage`。所有登录入口共用 storage helper，不在 URL、日志或公开文件中暴露凭据。

11. Locale：使用 UTF-8 和结构化方式修改，完成后解析 JSON 并检查异常字符。

12. 代码图谱：只用于导航，最终判断以源码、diff、测试和真实运行结果为准。

### 3.3 验收规则

采用最小充分验收，只检查本次改动直接影响的内容：

1. Markdown：检查内容差异、标题结构和 `git diff --check`，不启动项目或截图。

2. 项目文档：检查文档职责、链接和对应事实，不运行无关测试。

3. Locale：检查中英文对应项、JSON 解析和异常字符；显示逻辑变化时再打开页面。

4. 前端逻辑：运行相关 ESLint 或测试；模块、路由或打包方式变化时再运行构建。

5. UI：打开真实页面，检查受影响的尺寸和主题，只截取必要页面。

6. 路由与 PWA：检查 URL、页面显示、返回链、query/hash 和 Service Worker 导航。

7. 后端：运行相关测试，检查 API、权限、migration 和失败处理。

8. AI：根据改动选择结构、golden、live、压力或恢复测试；Mock 和 golden 不能代替真实 provider。

9. 微信采集：分别检查登录、抓取、解析、审核和发布，不能用登录成功代替采集成功。

10. 移动端：只验证受影响平台；发布结论需要对应工具、模拟器或真机证据。

11. OpenSpec：验证对应 change；只有多个 change 或正式 spec 受影响时才运行全量验证。

12. 生产发布：分别检查远端提交、GitHub Actions、实际部署版本、Caddy、服务状态和受影响流程的 smoke。构建、上传或健康检查成功不能单独证明新版本已在生产生效。

汇报时说明哪些检查已通过、哪些未运行、哪些是原有失败，以及哪些仍需真实环境或用户确认。

### 3.4 前端与多端规则

1. 路由动画：导航正确优先。URL 已变化但旧页面残留时，先检查退出动画和 `AnimatePresence`。

2. 深链参数：新增或迁移 query、hash 前，先检查所有使用位置，并验证重定向和返回链。

3. 复制与分享：提供 Web API、传统复制或手动文本降级；失败时必须给出可操作内容。

4. 移动弹层：同时检查 portal、transform、`100dvh`、滚动锁、安全区、返回行为和底部导航，不只增加 `z-index`。

5. 主题：先检查共享 token、DOM 和日夜分支，再做视觉验收，不用逐页颜色补丁解决系统问题。

6. PWA：根 Service Worker 不得接管 `/lingzhi/`、`/code/` 等独立应用。新增子应用时检查 Caddy、denylist、runtime cache 和旧 SW 控制下的导航。

7. 多端入口：保持同一个可见入口，运行时再选择 Web API、WebView 或原生桥。移动端次级操作需要检查打开、关闭、返回、遮罩和底栏遮挡。

8. 动效：优先复用共享 motion token 和 `prefers-reduced-motion`，不默认使用布局动画或高成本模糊。

### 3.5 正式材料

1. 修改项目文书、商业材料或演示稿前，先确认用户认可的最新可编辑源文件。DOCX 负责正文内容，PPTX 负责演示视觉，对应 PDF 只用于外观核验；旧版本只用于追溯和版式参考。

2. 正式材料必须遵守 3.1 的实体边界，不能把校园生态、数字平台、校内组织和商业公司写成同一个主体，也不能把合作资源写成公司自有资产。

### 3.6 未分类规则

本节保存暂时无法归类的稳定规则，始终放在第三章最后。当前暂无。
