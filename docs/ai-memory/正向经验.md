# 正向经验

本文件记录所有好的、值得保留或复用的内容，包括好回答、好判断、好执行方式、验证路径、工具组合、灵感启发和可复用框架。它是正反馈经验库：某次 Codex 做得好，或讨论中出现有价值的启发时，应沉淀“为什么好”和“下次怎么复用”。

使用边界：
- 好的、值得复用或继续发展的内容，写入本文件。
- 坏的、下次要避免的内容，写入 `错误经验.md`。
- 已经决定并会影响项目方向的历史路径，写入 `项目决策.md`。

## 2026-06-02 新增内容英文版同步

- 背景：跟进影像库、浙客松成果页英文翻译时，发现页面新增内容虽然主导航已英文，但部分静态数组、fallback 文案、空状态、SEO、ARIA 和按钮仍写死中文。
- 触发：用户明确要求以后凡是有新增内容都要自动跟进英文版。
- 过程：先用浏览器在英文模式下扫描 `/media`、`/hackathon`、`/hackathon/showcase` 的中文残留，再把页面文案接入 `public/locales/zh/translation.json` 与 `public/locales/en/translation.json`。
- 结论：任何新增用户可见内容都必须同步英文版，组件优先使用 `t(...)`，静态数组和 fallback 数据也要走翻译 key。
- 复用：后续修改页面、组件、空状态、Toast、SEO、ARIA、筛选字典或默认数据时，必须同时检查英文模式页面，避免只改中文。
- 位置：`AGENTS.md`、`public/locales/*/translation.json`

## 2026-06-02 macOS 本地前端验证运行时

- 背景：调整活动页右侧 AI 活动助手布局后，需要运行 lint、生产构建和活动助手 E2E。
- 触发：本机系统 PATH 没有 `npm`；使用 Codex App Node 或主运行时 Node 跑 `npm run build` 时，Rollup 原生模块 `rollup.darwin-arm64.node` 会因 macOS Team ID / library validation 不一致被拦截。
- 过程：先用独立 npm CLI 确认常规构建会失败，再复制 Raycast Node 到 `/tmp/zju-node-verify/node`，用 `codesign --force --sign - --entitlements` 加上 `com.apple.security.cs.disable-library-validation` 临时签名，然后直接调用 `node_modules/vite/bin/vite.js build`，绕过 npm script 重新切回 Codex App Node 的问题。Playwright 浏览器缺失时，用同一个临时 Node 执行 `node_modules/@playwright/test/cli.js install chromium`。
- 结论：这个仓库在当前 macOS 环境中，可靠验证命令是：
  - 单文件 lint：`./node_modules/.bin/eslint src/components/Events.jsx src/components/EventAssistantPanel.jsx e2e/event-assistant-flow.spec.js`
  - 构建：`/tmp/zju-node-verify/node node_modules/vite/bin/vite.js build`
  - E2E：`NO_PROXY=localhost,127.0.0.1,::1 /tmp/zju-node-verify/node node_modules/@playwright/test/cli.js test e2e/event-assistant-flow.spec.js --project=chromium`
- 复用：后续在本机遇到 `npm not found`、Rollup optional dependency “Cannot find module @rollup/rollup-darwin-arm64” 但 cause 是 `ERR_DLOPEN_FAILED` / Team ID mismatch 时，优先复用这套临时 Node 签名验证路径，不要误判为业务代码或依赖缺失。
- 位置：`docs/ai-memory/正向经验.md`

## 2026-06-23 AGENTS 规则文档改动记录

- 背景：升级 `AGENTS.md` 的空间机制后，用户指出文档虽然定义了意图、认知、问题、资料、方案五个空间，但空间之间的内在承接、因果和相互校准关系还不够显性。
- 触发：用户要求执行修改，并提醒每次改动前应备份 `AGENTS.md`，同时用文档日志记录规则文件的改动方法。
- 过程：修改前先将当前 `AGENTS.md` 备份为 `AGENTS.backup-20260623-005400.md`；随后在第 1 章补入“空间关系总则”，并给意图、认知、问题、资料、方案空间各补一条上下游关系说明。
- 结论：规则文档维护采用轻量方式：每次较大规则改动前先用时间戳备份 `AGENTS.md`；改动原因、范围和备份路径写入 `docs/ai-memory/正向经验.md`。只有涉及新功能、多模块改造、数据库/架构/高风险变更时，才使用 OpenSpec。
- 复用：后续修改 `AGENTS.md` 时，先备份，再改动，再在本文件追加一条记录，说明改动背景、触发、过程、结论、复用和位置。
- 位置：`AGENTS.md`、`AGENTS.backup-20260623-005400.md`、`docs/ai-memory/正向经验.md`

## 2026-06-23 AGENTS 重构式维护原则

- 背景：用户指出修改 `AGENTS.md` 不应停留在局部修补，而应把原文件当作原料，按“理解目的、提取思想、判断结构缺口、重组材料、检查行为改变”的方式重构。
- 触发：用户明确要求先备份，然后按重构式思维整体处理 `AGENTS.md`。
- 过程：修改前备份为 `AGENTS.backup-20260623-150624.md`；随后重写 `AGENTS.md` 的整体组织顺序，将文档改成“手册目的与阅读方式 -> 空间协作模型 -> 动作执行闭环 -> 项目记忆与规则重构”的结构，并把“规则重构原则”正式写入第 3 章。
- 结论：`AGENTS.md` 后续维护默认采用重构式方法：除错别字、路径或单句事实修正外，不做零散追加；先确认核心思想，再调整结构、标题、段落、例子和门禁，让规则能改变 AI 行为。
- 复用：后续修改 `AGENTS.md` 时，先备份，再判断是否需要重构；若是规则体系升级，优先保持概念完整性，而不是继续堆叠条款。
- 位置：`AGENTS.md`、`AGENTS.backup-20260623-150624.md`、`docs/ai-memory/正向经验.md`

## 2026-06-23 AGENTS 空间输出格式校准

- 背景：测试发现严格遵守 `AGENTS.md` 后，空间判断更完整，但外部表达可能过于隐形；用户希望复杂问题能分点呈现“意图空间 / 认知空间”等已激活空间。
- 触发：用户指出不应新增孤立规则，而应统筹修改控制输出格式的原有语句。
- 过程：修改前备份为 `AGENTS.backup-20260623-152322.md`；随后统一调整手册阅读方式、两阶段流程、空间门禁、空间裁剪和反误用段落，把“不要机械输出标题”改为“不要机械输出全部空间标题，但复杂问题应显式呈现本轮激活空间”。
- 结论：空间机制的外部表达原则是：简单问题可裁剪并直接回答；复杂讨论、规则设计、产品判断、项目分析和方案制定时，应优先用编号小标题展示实际激活的空间，既防机械套格式，也防隐藏判断结构。
- 复用：后续调整 `AGENTS.md` 表达规则时，先查找会控制输出格式的原句，优先系统修改既有规则，避免追加孤立条款。
- 位置：`AGENTS.md`、`AGENTS.backup-20260623-152322.md`、`docs/ai-memory/正向经验.md`

## 2026-06-23 AGENTS 语言规则边界校准

- 背景：用户希望调整的是 `AGENTS.md` 中“与语言有关的规则”，而不是把整份文档里的技术术语都做中文润色。
- 触发：上一轮误把正文中的 `prompt`、`Agent`、`lint`、`smoke`、`URL`、`PATH` 等技术语境表达中文化，用户指出真实意图是修改语言规则本身。
- 过程：保留 `AGENTS.backup-20260623-153633.md` 作为上一轮过度中文化前的备份；随后在 2026-06-24 再备份为 `AGENTS.backup-20260624-000514.md`，并将 `0.2 语言与文档` 改成边界规则：默认中文用于回复、文档、任务说明和用户可见文案，不要求强行翻译技术术语、工具名、文件名、命令、代码标识或行业惯用表达。
- 结论：`AGENTS.md` 的语言策略是：规则语义和面向用户的解释使用中文；技术术语、工具名、文件名、路径、命令、代码标识和行业惯用表达可以保留英文或中英混用。不要为了“中文化”牺牲准确性、可检索性和工程习惯。
- 复用：后续用户提到“中文优化”时，先判断是在改语言规则、改用户可见文案，还是改文档正文风格；不要默认全文件词汇替换。
- 位置：`AGENTS.md`、`AGENTS.backup-20260623-153633.md`、`AGENTS.backup-20260624-000514.md`、`docs/ai-memory/正向经验.md`

## 2026-06-24 OpenSpec 收尾责任

- 背景：整理 OpenSpec 后确认它不会自动判断 change 是否完成，也不会自动归档。
- 触发：用户指出 OpenSpec 是 Codex 自己使用的任务工具，因此 Codex 应该负责归档和收尾，不能把维护成本留给用户。
- 过程：修改前备份为 `AGENTS.backup-20260624-154408.md`；随后在 `2.1.4 选择规划与执行路径` 的 OpenSpec 流程后补入收尾责任：已完成并验证通过则归档并重新 validate；不能归档则汇报保留原因和剩余任务。
- 结论：Codex 创建、修改或依赖 OpenSpec change 推进任务时，收束阶段必须同步处理 OpenSpec 状态。
- 复用：后续所有 OpenSpec 任务都按“创建/使用 -> 执行 -> 验证 -> 归档或说明保留原因”的闭环处理。
- 位置：`AGENTS.md`、`AGENTS.backup-20260624-154408.md`、`docs/ai-memory/正向经验.md`

## 2026-06-25 平台底座统一改造第一版

- 背景：用户要求不要把 20 个优化点逐个小修小补，而是把项目朝“架构统一、框架清晰”的方向推进。
- 触发：执行 `unify-platform-foundation` OpenSpec change，第一版限定为不引入微服务、不切数据库、不重写 TypeScript、不更换 UI 框架。
- 过程：先建立 `docs/architecture/platform-foundation.md` 和 `src/app`、`src/features`、`src/shared` 目录边界；再把可复用 migration helper 移出 controller，移除 tag/profile-card controller 的运行时 schema 兜底；随后统一错误响应形状、限制 axios 写操作自动重试、把 token 默认存入 `sessionStorage`、新增 body-level portal 并迁移 ProjectPlaza 详情弹层；最后把 AI JSON runtime 响应组装和搜索轻量词元索引边界写清。
- 结论：底座第一版的稳定规则是：schema 只进 migration，controller 不做隐形迁移；写操作默认不自动重试；登录 token 默认 session-only；移动端全屏弹层优先走 shared body portal；`resource_search_index.vector_json` 只能称为轻量词元索引，不能宣称 embedding。
- 复用：后续拆 Events、UploadModal、PublicProfile、Admin、AI assistant 或搜索能力时，先沿用这些底座目录和规则；新增领域迁移时使用 `server/src/config/migrations/helpers.js`；新增移动端弹层时优先使用 `src/shared/ui/BodyPortal.jsx` 并补 body portal/scroll-lock 验证。
- 位置：`docs/architecture/platform-foundation.md`、`server/src/config/migrations/helpers.js`、`src/shared/authTokenStorage.js`、`src/shared/ui/BodyPortal.jsx`、`openspec/specs/platform-foundation/spec.md`

## 2026-06-25 项目记忆正反馈命名校准

- 背景：用户指出原 `工作流优化.md` 容易被理解成狭义流程提效，但真实意图是记录 Codex 某次回答、判断或执行为什么做得好，并让这种好行为下次可复现。
- 触发：讨论对比 `错误经验`、`项目决策`、`灵感涌现` 后，确认项目记忆需要一个明确的正反馈文件，与错误经验形成一正一反。
- 过程：修改前备份 `AGENTS.md` 为 `AGENTS.backup-20260625-203425.md`；随后将 `docs/ai-memory/工作流优化.md` 重命名为 `docs/ai-memory/成功模式.md`，并同步调整 `AGENTS.md` 与 `docs/README.md` 中的分类定义和读取规则。
- 结论：`成功模式.md` 更名为 `正向经验.md` 后，记录范围扩大为所有好的、值得复用或继续发展的内容，包括成功做法、灵感启发和可复用判断框架。
- 复用：后续当用户明确表示“这次回答很好”“这个过程很好”“这个判断方式值得保留”，或讨论中出现值得以后复用的启发时，优先沉淀到 `正向经验.md`；只有经过多次验证、足够稳定且能显著改变 Codex 行为时，才上升为 `AGENTS.md` 规则。
- 位置：`AGENTS.md`、`AGENTS.backup-20260625-203425.md`、`docs/README.md`、`docs/ai-memory/正向经验.md`

## 2026-06-26 项目记忆三分类收束

- 背景：继续讨论项目记忆后，用户指出“灵感涌现”可以放进正向经验，好的东西统一记录，坏的东西统一记录，项目决策作为项目历史和决策路径。
- 触发：四文件体系里的 `灵感涌现.md` 过于抽象，AI 执行时容易在“灵感、成功模式、项目决策”之间犹豫，且当前文件也没有实质条目。
- 过程：修改前备份 `AGENTS.md` 为 `AGENTS.backup-20260626-220207.md`；随后将 `docs/ai-memory/成功模式.md` 重命名为 `docs/ai-memory/正向经验.md`，删除空壳 `docs/ai-memory/灵感涌现.md`，并同步调整 `AGENTS.md` 与 `docs/README.md` 的分类定义。
- 结论：项目记忆收束为三类：`正向经验.md` 记录好的、值得保留或复用的内容；`错误经验.md` 记录坏的、需要避免重犯的内容；`项目决策.md` 记录项目历史和决策路径。
- 复用：后续沉淀时先按“好 / 坏 / 决策历史”三分法归档；不要为还没形成规模的抽象类型新增独立文件。
- 位置：`AGENTS.md`、`AGENTS.backup-20260626-220207.md`、`docs/README.md`、`docs/ai-memory/正向经验.md`
