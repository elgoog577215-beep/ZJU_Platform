## 1. OpenSpec 与数据边界

- [x] 1.1 校验本变更 proposal、design、spec、tasks，确认范围不包含精选和比赛成果联动。
- [x] 1.2 检查现有图片、视频、上传、后台资源管理和导航实现，确认最小改动点。

## 2. 后端分类能力

- [x] 2.1 新增 `media_categories` 核心 schema 与运行迁移。
- [x] 2.2 为 `photos` 与 `videos` 增加 `category_id` 字段和必要索引。
- [x] 2.3 新增影像分类控制器与公开/后台 API。
- [x] 2.4 扩展图片与视频资源字段，支持创建、更新和按 `category_id` 查询。

## 3. 前台影像库

- [x] 3.1 新增 `/media` 影像库页面，桌面端左侧图片 2/3、右侧视频 1/3。
- [x] 3.2 实现分类筛选，同时刷新图片和视频区域。
- [x] 3.3 实现移动端上下布局和空状态。
- [x] 3.4 更新导航与首页入口，将主入口收束为“影像库”，保留旧路由兼容。

## 4. 上传与后台管理

- [x] 4.1 更新图片/视频上传弹窗，允许选择启用的影像分类。
- [x] 4.2 更新后台图片和视频资源管理，显示并编辑分类字段。
- [x] 4.3 新增后台影像分类管理入口，支持创建、编辑、启停和软删除。

## 5. 验证与收束

- [x] 5.1 运行 `openspec validate --all`。
- [x] 5.2 运行后端相关测试或至少启动级检查。
- [x] 5.3 运行前端构建检查。
- [x] 5.4 启动本地站点，浏览器验证 `/media` 桌面端和移动端布局、上传分类选择、后台分类管理。
- [x] 5.5 根据验证结果完成必要修正，并更新任务状态。
