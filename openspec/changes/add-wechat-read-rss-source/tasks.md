# add-wechat-read-rss-source 任务

## 1. 规格与契约

- [ ] 1.1 完成来源类型、feed ID、基地址和无凭据边界的规格评审。
- [x] 1.2 增加 Atom/RSS 字段映射与正文缺失策略的测试 fixture。

## 2. 后端来源适配

- [x] 2.1 新增 WeRead RSS 请求、基地址/feed ID 校验和超时错误归一化服务。
- [x] 2.2 新增 Atom/RSS 解析器，输出现有增量文章服务可消费的统一文章形状。
- [x] 2.3 为 `wechat_mp_ingest_accounts` 增加 `source_type`、`rss_feed_id` 兼容迁移和序列化字段。
- [x] 2.4 将定时/手动任务按来源分发，RSS 来源不再调用直连微信 MP 正文接口。
- [x] 2.5 保持去重、全文保存、AI 提取、活动初筛、运行错误和重试语义不变。
- [x] 2.6 将启用的 `wewe_rss` 来源排在直连微信 MP 来源之前执行，同时保留旧来源兼容性。

## 3. 管理后台

- [x] 3.1 在现有公众号来源表单增加直连微信 MP / 微信读书 RSS 选择。
- [x] 3.2 增加 feed ID、RSS 配置提示和不要求扫码登录的状态文案。
- [x] 3.3 同步中英文 locale，并验证键值和异常字符。
- [x] 3.4 保持来源启停、定时运行、候选文章和解析重试入口兼容。
- [x] 3.5 将来源表单默认值设为 `wewe_rss`，并在中英文说明中标记 RSS 为主要来源。

## 4. 验证与上线

- [x] 4.1 运行来源解析、校验和任务分支测试。
- [x] 4.2 运行现有微信 MP scheduled ingest 相关测试。
- [x] 4.3 运行定向 lint 和 `npm run build`；全量 lint 受既有 `app/ios-capacitor` 构建产物错误阻断。
- [ ] 4.4 在 WeWe RSS 中配置至少一个真实 feed 后执行一次手动生产 smoke。
- [ ] 4.5 记录数据库备份、回滚方式和真实 feed 缺失/过期时的恢复动作。
- [x] 4.6 验证 RSS 来源默认值、来源排序和直连来源兼容执行顺序。
