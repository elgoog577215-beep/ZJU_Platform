# 任务

- [ ] 扩展文档导入器，支持 TXT、HTML 和 CLI 文件夹入口文件。
- [ ] 新增后端 CLI 控制器，提供 `/api/cli/import`、`/api/cli/publish`、`/api/cli/submissions`。
- [ ] 在路由中挂载受认证保护的 CLI 接口，并保留上传白名单和大小限制。
- [ ] 新增 `zju` CLI 工具，实现登录、身份、预览、发布、状态和退出命令。
- [ ] 补充部署/使用说明，明确服务器需要开放 HTTPS、上传体积和 `/api` 访问。
- [ ] 运行语法、导入解析和接口级验证。
