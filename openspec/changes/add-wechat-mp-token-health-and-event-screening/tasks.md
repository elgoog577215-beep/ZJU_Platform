# 公众号登录态验活与活动候选筛选任务

## 1. OpenSpec

- [x] 1.1 新增 proposal、design、tasks 和 spec delta。
- [x] 1.2 尝试运行 OpenSpec validate；当前环境缺少 CLI，已记录验证限制。

## 2. Token 验活

- [x] 2.1 新增轻量验活请求、结果分类和短时缓存。
- [x] 2.2 将真实请求成功/失效结果同步到验活状态。
- [x] 2.3 扩展管理员状态接口并保持凭据脱敏。
- [x] 2.4 在公众号导入页自动轮询并展示有效、过期和暂时不可确认状态。

## 3. 活动候选筛选

- [x] 3.1 扩展 `wechat_event_parse` 输出活动候选字段和规则。
- [x] 3.2 新增增量文章活动筛选状态与活动关联字段迁移。
- [x] 3.3 将通过筛选的解析结果幂等写入 `events`，状态固定为 `pending`。
- [x] 3.4 保留非候选文章、原因和失败重试能力。

## 4. 验证

- [x] 4.1 补充 token 验活分类、缓存和网页状态测试。
- [x] 4.2 补充非活动拒绝、活动 pending 入库和重复采集幂等测试。
- [x] 4.3 运行微信 MP 测试、AI 检查、lint 和生产构建；OpenSpec CLI 因环境缺失未能执行。
