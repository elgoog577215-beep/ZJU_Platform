## 1. 后端统一搜索

- [x] 1.1 新增全站搜索服务，解析自然语言查询并输出结构化意图。
- [x] 1.2 检索活动、AI 社区、影像库三类内容，返回统一结果模型。
- [x] 1.3 将 `/api/search` 接到统一搜索服务，并保留兼容字段。
- [x] 1.4 在 AI agent registry 中记录全站搜索作为独立搜索能力，不替代活动推荐助手。

## 2. 前端全站入口

- [x] 2.1 升级 `SearchPalette`，展示分组结果、匹配理由和搜索解析摘要。
- [x] 2.2 修正社区结果过滤，让 AI 社区进入全站搜索结果。
- [x] 2.3 打通活动、AI 社区、影像库的结果跳转。
- [x] 2.4 为影像库补充 `photo` / `video` 深链处理。

## 3. 验证

- [x] 3.1 运行相关 JS 语法检查。
- [x] 3.2 运行 AI agent registry 检查。
- [x] 3.3 运行前端构建。
- [x] 3.4 对 `/api/search` 做本地接口烟测。

## 4. 用户画像与真实数据推广

- [x] 4.1 将活动推荐画像编辑入口迁入用户系统设置页。
- [x] 4.2 推荐助手改为读取用户系统画像，并引导用户去个人资料维护长期偏好。
- [x] 4.3 为 `user_event_preferences` 增加空闲时间字段，并纳入推荐画像摘要。
- [x] 4.4 用真实数据库修正全站搜索召回：活动类型词做语义扩展，影像库兼容旧分类表。
- [x] 4.5 后续接入治理助手生成结构化摘要和向量索引。

## 5. 全站结构化索引

- [x] 5.1 新增 `resource_search_index`，覆盖活动、AI 社区、影像库公开资源。
- [x] 5.2 新增索引刷新服务和脚本，生成摘要、关键词、分面字段和向量兼容字段。
- [x] 5.3 全站搜索合并结构化索引补充召回，同时保留原 SQL 召回。
- [x] 5.4 将全站索引覆盖率接入 AI 助手总览和 agent registry。
- [x] 5.5 用真实数据库运行迁移、索引刷新和搜索烟测。
