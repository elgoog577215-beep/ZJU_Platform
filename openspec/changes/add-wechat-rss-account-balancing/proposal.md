# 微信读书 RSS 多账号轮询

## 背景

WeWe RSS 原有实现随机选择最多 10 个启用账号，短期分配不均；每日限流名单首次写入缺失，429 账号未被正确排除。

## 变更

在独立 wewe-rss 服务中按账号 ID 轮询所有启用账号，修复每日限流排除，保留现有采集间隔和文章读取重试次数。主平台继续使用现有账号管理代理、采集与审核链。

## 影响

无 API、数据库或前端变化。适用于单个服务进程；需单独发布 wewe-rss 后生效。
